# 课程习题测验系统 — 面试准备：技术原理与面试题

> 本文档全面梳理项目所用技术栈、核心原理，以及面试官可能根据此项目提出的问题，供面试复习使用。

---

## 目录

1. [项目概述](#1-项目概述)
2. [技术栈全景图](#2-技术栈全景图)
3. [核心技术原理深度解析](#3-核心技术原理深度解析)
   - [3.1 Spring Boot 自动配置](#31-spring-boot-自动配置)
   - [3.2 MyBatis ORM 与动态 SQL](#32-mybatis-orm-与动态-sql)
   - [3.3 Session/Cookie 身份认证](#33-sessioncookie-身份认证)
   - [3.4 拦截器（Interceptor）机制](#34-拦截器interceptor机制)
   - [3.5 RBAC 权限模型](#35-rbac-权限模型)
   - [3.6 全局异常处理](#36-全局异常处理)
   - [3.7 BCrypt 密码加密](#37-bcrypt-密码加密)
   - [3.8 事务管理](#38-事务管理)
   - [3.9 数据库设计与外键策略](#39-数据库设计与外键策略)
   - [3.10 自动评分算法](#310-自动评分算法)
   - [3.11 RESTful API 设计](#311-restful-api-设计)
4. [面试题分类汇总](#4-面试题分类汇总)
   - [4.1 Java 基础](#41-java-基础)
   - [4.2 Spring Boot 框架](#42-spring-boot-框架)
   - [4.3 MyBatis / 数据库](#43-mybatis--数据库)
   - [4.4 安全与认证](#44-安全与认证)
   - [4.5 系统设计](#45-系统设计)
   - [4.6 项目实战](#46-项目实战)
   - [4.7 前端](#47-前端)
5. [高频追问清单](#5-高频追问清单)

---

## 1. 项目概述

课程习题测验系统 是一个基于 **Spring Boot + MyBatis + MySQL + 纯前端 HTML/JS** 的在线教学测验平台，支持三种角色：

| 角色 | 核心功能 |
|------|---------|
| **管理员（Admin）** | 管理教师/学生账号、课程分配、查看课程内容 |
| **教师（Teacher）** | 管理课程题库（单选/多选/判断/填空/简答）、组卷、发布/回收试卷、查看学生成绩 |
| **学生（Student）** | 课程练习（随机抽题、即时反馈）、参加考试（限时答题、自动判分）、查看成绩与错题解析 |

**核心业务流程：**
- **练习流程**：学生选课程 → 随机抽题 → 逐题作答 → 确认答案 → 即时反馈（✓/✗ + 解析） → 下一题
- **考试流程**：学生选试卷 → 开始考试 → 限时答题 → 提交 → 自动判分 → 查看成绩报告
- **内容管理**：教师创建题目（支持5种题型） → 组卷（选题 + 设分值） → 发布 → 学生可见

---

## 2. 技术栈全景图

### 后端

| 技术 | 版本 | 作用 |
|------|------|------|
| **Java** | 26 | 编程语言 |
| **Spring Boot** | 4.0.7 | 应用框架（IoC容器、自动配置、Web MVC） |
| **MyBatis** | 4.0.1（mybatis-spring-boot-starter） | ORM/数据访问框架，XML映射SQL |
| **MySQL** | 8.x | 关系型数据库 |
| **MySQL Connector/J** | - | JDBC驱动 |
| **spring-security-crypto** | - | BCrypt密码编码器（独立使用，非完整Spring Security） |
| **Lombok** | 1.18.38 | 编译期代码生成（@Data/@Getter/@Setter） |
| **Maven** | 3.x | 项目构建与依赖管理 |
| **Jakarta Servlet** | - | HttpSession管理 |

### 前端

| 技术 | 说明 |
|------|------|
| **HTML5 + CSS3** | 13个静态页面，由Spring Boot直接Serve |
| **Vanilla JavaScript（ES5/ES6）** | 纯原生JS，无前端框架（React/Vue） |
| **Tailwind CSS** | 通过CDN加载（`cdn.tailwindcss.com`） |
| **Iconify Icon** | 自托管（`iconify-icon.min.js`），因jsdelivr在国内被墙 |
| **Cookie/Session** | 前端通过 `credentials: 'include'` 携带会话Cookie |

### 开发与部署

| 工具 | 说明 |
|------|------|
| **Maven** | `mvn clean package -DskipTests` 构建可执行JAR |
| **嵌入式Tomcat** | Spring Boot内嵌，`java -jar` 直接运行 |
| **单端口部署** | 前后端统一部署在8080端口 |

---

## 3. 核心技术原理深度解析

### 3.1 Spring Boot 自动配置

**原理**：`@SpringBootApplication` 是一个组合注解，包含：
- `@SpringBootConfiguration`：标识为配置类
- `@EnableAutoConfiguration`：通过 `spring-boot-autoconfigure` 模块的 `META-INF/spring/org.springframework.boot.autoconfigure.AutoConfiguration.imports` 文件，自动加载所有自动配置类
- `@ComponentScan`：扫描启动类同级及子包下的 `@Component`/`@Service`/`@Controller` 等

**本项目中的应用**：
- `spring-boot-starter-web` 自动配置了 DispatcherServlet、HttpMessageConverter（Jackson JSON序列化）、嵌入式Tomcat
- `mybatis-spring-boot-starter` 自动配置了 SqlSessionFactory、MapperScanner
- `application.yml` 中的 `spring.datasource.*` 被自动装配为 DataSource Bean
- `@MapperScan("com.examsystem.mapper")` 扫描所有 Mapper 接口生成代理对象

**面试常问**：Spring Boot 的自动配置原理是什么？`@SpringBootApplication` 做了什么？

---

### 3.2 MyBatis ORM 与动态 SQL

**原理**：MyBatis 通过 XML 映射文件或注解将 Java 方法与 SQL 语句绑定：
1. `SqlSessionFactory` 读取 XML 配置，解析 `<mapper>` 中的 SQL
2. `MapperProxy`（JDK动态代理）拦截 Mapper 接口方法调用
3. 根据方法全限定名（`namespace.id`）匹配对应的 SQL
4. 执行 SQL，通过 `ResultMap` 映射结果集到 Java 对象

**本项目中的关键 MyBatis 特性**：

```xml
<!-- 1. 动态SQL：根据条件拼接 WHERE 子句 -->
<select id="findByFilters" resultMap="BaseResultMap">
    SELECT * FROM t_question
    <where>
        <if test="courseId != null">AND course_id = #{courseId}</if>
        <if test="questionType != null">AND question_type = #{questionType}</if>
        <if test="difficulty != null">AND difficulty = #{difficulty}</if>
        <if test="keyword != null and keyword != ''">
            AND content LIKE CONCAT('%', #{keyword}, '%')
        </if>
    </where>
    ORDER BY create_time DESC
    LIMIT #{offset}, #{limit}
</select>

<!-- 2. 批量插入 -->
<insert id="batchInsert" useGeneratedKeys="true" keyProperty="answerId">
    INSERT INTO t_exam_answer (...) VALUES
    <foreach collection="list" item="item" separator=",">
        (#{item.examRecordId}, ...)
    </foreach>
</insert>

<!-- 3. 自动生成主键回填 -->
<insert id="insert" useGeneratedKeys="true" keyProperty="questionId">
    INSERT INTO t_question (...) VALUES (...)
</insert>

<!-- 4. 多表 JOIN + ResultMap 映射 -->
<select id="selectById" resultMap="BaseResultMap">
    SELECT q.*, c.course_name, t.real_name AS teacher_name
    FROM t_question q
    LEFT JOIN t_course c ON q.course_id = c.course_id
    LEFT JOIN t_teacher t ON q.teacher_id = t.teacher_id
    WHERE q.question_id = #{questionId}
</select>

<!-- 5. 随机抽题（MySQL RAND()） -->
<select id="findRandom" resultMap="BaseResultMap">
    SELECT * FROM t_question WHERE course_id = #{courseId}
    ORDER BY RAND() LIMIT #{count}
</select>
```

**配置要点**：
```yaml
mybatis:
  mapper-locations: classpath:mapper/*.xml      # XML文件位置
  type-aliases-package: com.examsystem.entity   # 实体类别名
  configuration:
    map-underscore-to-camel-case: true          # 自动驼峰转换（question_type → questionType）
```

**面试常问**：MyBatis 和 Hibernate 的区别？`#{}` vs `${}` 的区别？MyBatis 二级缓存？动态SQL怎么实现的？

---

### 3.3 Session/Cookie 身份认证

**原理**：
1. 用户登录 → 后端验证账号密码 → 创建 `HttpSession`，存入 `userId`、`role`、`realName`、`username`
2. 响应头中 `Set-Cookie: QUIZ_SESSION=<sessionId>; HttpOnly`（`HttpOnly` 防止JS读取Cookie，防御XSS）
3. 后续请求自动携带Cookie → 后端通过 `request.getSession(false)` 获取Session → 读取用户信息
4. 登出 → `session.invalidate()` 销毁Session

**本项目配置**：
```yaml
server:
  servlet:
    session:
      timeout: 30m           # Session 30分钟无操作过期
      cookie:
        name: QUIZ_SESSION   # 自定义Cookie名（不暴露默认JSESSIONID）
        http-only: true      # JS无法读取，防御XSS窃取Cookie
```

**为什么不使用 JWT？**
- 本项目是传统的服务端渲染/单服务器架构，无跨域认证需求
- Session方案更简单，天然支持服务端主动失效（登出即销毁）
- 不需要JWT的无状态优势（本项目只有一台服务器）

**面试常问**：Session和JWT的区别？Cookie的HttpOnly/Secure/SameSite属性？如何防止Session劫持？

---

### 3.4 拦截器（Interceptor）机制

**原理**：Spring MVC 拦截器是基于 `HandlerInterceptor` 接口的AOP机制：
- `preHandle`：在Controller方法执行前调用，返回 `true` 放行、`false` 拦截
- `postHandle`：在Controller执行后、视图渲染前调用
- `afterCompletion`：在请求完成后调用（包括异常情况）

**本项目中的拦截器链**：

```
请求 /api/teacher/questions
  ↓
LoginInterceptor.preHandle()
  ├─ session存在? → 否 → 返回401 JSON
  └─ session存在? → 是 → 放行
  ↓
RoleInterceptor.preHandle()
  ├─ role == "teacher"? → 否 → 返回403 JSON
  └─ role == "teacher"? → 是 → 放行
  ↓
Controller 执行
```

**配置注册**（`WebMvcConfig`）：
```java
// LoginInterceptor 拦截所有 /api/**，排除登录接口
registry.addInterceptor(loginInterceptor)
    .addPathPatterns("/api/**")
    .excludePathPatterns("/api/auth/login");

// RoleInterceptor 仅拦截角色路径
registry.addInterceptor(roleInterceptor)
    .addPathPatterns("/api/admin/**", "/api/teacher/**", "/api/student/**")
    .excludePathPatterns("/api/auth/login");
```

**面试常问**：拦截器（Interceptor）和过滤器（Filter）的区别？AOP的实现方式有哪些？拦截器的执行顺序？

---

### 3.5 RBAC 权限模型

**本项目使用的双层权限控制**：

**第一层：URL路径拦截（RoleInterceptor）**
```
/api/admin/**   → 仅 role=admin
/api/teacher/** → 仅 role=teacher
/api/student/** → 仅 role=student
```

**第二层：数据级权限过滤（课程隔离）**
- **教师**：只能看到自己分配课程（`t_teacher_course`）下的题目和试卷
  ```java
  // TeacherQuestionController 中获取教师课程列表
  List<Long> courseIds = teacherCourseMapper.selectCourseIdsByTeacherId(teacherId);
  // 查询时传入 courseIds 过滤
  questionService.listQuestions(courseId, type, diff, keyword, page, size, courseIds);
  ```
- **学生**：只能看到自己已选课程（`t_student_course`）的练习和考试
  ```java
  // PracticeServiceImpl 中校验课程权限
  List<Long> enrolledCourseIds = studentCourseMapper.selectCourseIdsByStudentId(studentId);
  if (!enrolledCourseIds.contains(config.getCourseId())) {
      throw new BusinessException("您没有选修该课程");
  }
  ```

**关键设计决策**：`t_question.teacher_id` 和 `t_paper.teacher_id` 仅为展示字段，无外键约束，**不作为权限过滤依据**。这确保了教师离职后，其创建的题目/试卷仍可被同课程的其他教师使用。

**面试常问**：RBAC是什么？你们怎么实现细粒度权限控制的？如果教师A和教师B教同一门课怎么处理？

---

### 3.6 全局异常处理

**实现**：`@RestControllerAdvice` + `@ExceptionHandler`

```java
@RestControllerAdvice  // = @ControllerAdvice + @ResponseBody
public class GlobalExceptionHandler {

    @ExceptionHandler(BusinessException.class)
    public Result<?> handleBusinessException(BusinessException e) {
        return Result.error(e.getCode(), e.getMessage());  // 返回统一JSON格式
    }

    @ExceptionHandler(Exception.class)
    public Result<?> handleException(Exception e) {
        log.error("Unexpected error", e);
        return Result.error(500, "服务器内部错误");  // 兜底，不暴露内部细节
    }
}
```

**自定义业务异常**：
```java
@Getter
public class BusinessException extends RuntimeException {
    private final Integer code;  // 默认400
    // 支持自定义错误码和消息
}
```

**面试常问**：`@ControllerAdvice` 和 `@RestControllerAdvice` 的区别？异常处理会改变HTTP状态码吗？如何处理校验异常（如 `@Valid` 校验失败）？

---

### 3.7 BCrypt 密码加密

**原理**（`spring-security-crypto` 的 `BCryptPasswordEncoder`）：
1. **加盐（Salt）**：自动生成16字节随机盐值
2. **哈希**：基于Blowfish加密算法，迭代2^logRounds次（默认10轮，即1024次）
3. **输出格式**：`$2a$10$<22字符salt><31字符hash>`，盐值直接嵌在密文中
4. **验证**：`encoder.matches(rawPassword, encodedPassword)` 从密文中提取盐值，对传入的密码用相同盐值哈希后比对

**为什么选BCrypt而不是MD5/SHA-256？**
- BCrypt专为密码哈希设计，**慢哈希**特性使暴力破解成本极高
- 每个密码自带独立盐值，彩虹表攻击无效
- `spring-security-crypto` 提供了久经考验的实现

**本项目中的用法**：
```java
public class PasswordUtil {
    private static final BCryptPasswordEncoder encoder = new BCryptPasswordEncoder();

    public static String encode(String rawPassword) {
        return encoder.encode(rawPassword);       // 注册/重置密码时
    }

    public static boolean matches(String rawPassword, String encodedPassword) {
        return encoder.matches(rawPassword, encodedPassword);  // 登录验证时
    }
}
```

**注意**：`BCryptPasswordEncoder` 是线程安全的单例，本项目用 `static final` 持有。

**面试常问**：为什么要对密码加盐？BCrypt vs Argon2？彩虹表攻击是什么？MD5为什么不适合密码存储？

---

### 3.8 事务管理

**本项目使用 `@Transactional` 声明式事务**：

```java
@Transactional  // 默认传播行为 REQUIRED，回滚 RuntimeException
public void deleteQuestion(Long questionId) {
    // 必须先删除FK关联表中的数据，再删主表
    paperQuestionMapper.deleteByQuestionId(questionId);   // 1. 试卷关联
    practiceRecordMapper.deleteByQuestionId(questionId);   // 2. 练习记录
    examAnswerMapper.deleteByQuestionId(questionId);       // 3. 考试答案
    questionOptionMapper.deleteByQuestionId(questionId);   // 4. 选项
    questionMapper.deleteById(questionId);                 // 5. 题目本身
}
```

**为什么需要事务？** 如果其中一步失败（如网络中断、SQL错误），之前的删除操作必须回滚，否则数据库处于不一致状态。

**Spring 事务原理简述**：
1. `@Transactional` 被 `TransactionInterceptor` 拦截
2. 方法执行前：通过 `DataSourceTransactionManager` 开启事务（`conn.setAutoCommit(false)`）
3. 方法正常返回：提交事务（`conn.commit()`）
4. 方法抛 `RuntimeException`/`Error`：回滚事务（`conn.rollback()`）

**面试常问**：事务的ACID特性？事务传播行为（Propagation）？`@Transactional` 失效的常见场景？MySQL的隔离级别？

---

### 3.9 数据库设计与外键策略

**13张表设计**：

```
账号体系（3张）
├── t_admin         管理员
├── t_teacher       教师
└── t_student       学生

教学内容（5张）
├── t_course         课程
├── t_question       习题
├── t_question_option 习题选项
├── t_paper          试卷
└── t_paper_question 试卷-习题关联（多对多）

学习记录（3张）
├── t_practice_record 练习记录
├── t_exam_record     考试记录（主表）
└── t_exam_answer     考试答题明细

课程关联（2张）
├── t_teacher_course  教师-课程（多对多）
└── t_student_course  学生-课程（多对多）
```

**关键外键策略**：

| 关系 | FK策略 | 原因 |
|------|--------|------|
| `t_question.teacher_id` | **无FK约束** | 教师删除后题目保留，供同课程其他教师使用 |
| `t_paper.teacher_id` | **无FK约束** | 同上 |
| `t_practice_record.student_id` | `ON DELETE SET NULL` | 学生删除后保留练习数据，student_id设为NULL |
| `t_exam_record.student_id` | `ON DELETE SET NULL` | 同上 |
| `t_paper_question.question_id` | 默认（`ON DELETE RESTRICT`） | 被试卷引用的题目不可删除，必须先清理关联 |
| `t_teacher_course` | 默认 | 删除中间表记录不影响教师和课程 |

**一次性删除题目前必须先清理4张FK引用表**（本项目 `QuestionServiceImpl.deleteQuestion()` 中实现）：
```
t_paper_question → t_practice_record → t_exam_answer → t_question_option → t_question
```

**面试常问**：外键的几种删除策略（CASCADE/SET NULL/RESTRICT）？你为什么有些字段不用外键？索引怎么设计？

---

### 3.10 自动评分算法

**本项目核心算法 — `checkAnswer()`**：

```java
private boolean checkAnswer(String studentAnswer, String correctAnswer, Integer questionType) {
    if (studentAnswer == null || correctAnswer == null) return false;
    switch (questionType) {
        case 1: // 单选题 — 精确匹配（忽略大小写）
        case 3: // 判断题 — 精确匹配
            return studentAnswer.trim().equalsIgnoreCase(correctAnswer.trim());
        case 2: // 多选题 — 排序后比较（"A,C" = "C,A"）
            return sortChars(studentAnswer.trim().toUpperCase())
                    .equals(sortChars(correctAnswer.trim().toUpperCase()));
        default: // 填空/简答 — 精确匹配，实际业务中通常人工批改
            return studentAnswer.trim().equalsIgnoreCase(correctAnswer.trim());
    }
}

// 多选排序辅助方法
private String sortChars(String s) {
    return s.chars().sorted()
            .collect(StringBuilder::new, StringBuilder::appendCodePoint, StringBuilder::append)
            .toString();
}
```

**评分策略**：
- **单选题/判断题**：精确匹配得分，否则0分
- **多选题**：将学生答案和正确答案都排序后比较（如学生"B,A"排序为"A,B"，与答案"A,B"匹配）
- **填空/简答**：精确匹配，但答案存入数据库供人工复核
- **练习模式**：每题即时反馈，不计总分
- **考试模式**：每题按 `t_paper_question.score` 计分，汇总为 `total_score`

**面试常问**：多选题评分为什么排序？怎么处理大小写和空格？如果要做模糊匹配（如填空允许同义词）怎么改？

---

### 3.11 RESTful API 设计

**URL设计规范**：

```
GET    /api/admin/teachers          列表
GET    /api/admin/teachers/{id}     详情
POST   /api/admin/teachers          创建
PUT    /api/admin/teachers/{id}     更新
PUT    /api/admin/teachers/{id}/status   局部更新（启用/禁用）
PUT    /api/admin/teachers/{id}/reset-password  动作型接口
DELETE /api/admin/teachers/{id}     删除
GET    /api/admin/teachers/{id}/courses    子资源
PUT    /api/admin/teachers/{id}/courses    子资源更新
```

**统一响应格式**：
```json
{
    "code": 200,
    "message": "success",
    "data": { ... }
}
```

**分页响应**：
```json
{
    "code": 200,
    "message": "success",
    "data": {
        "list": [...],
        "total": 50,
        "page": 1,
        "pageSize": 10,
        "totalPages": 5
    }
}
```

**面试常问**：RESTful API的设计原则？POST和PUT的区别？为什么要统一响应格式？分页怎么设计？

---

## 4. 面试题分类汇总

### 4.1 Java 基础

#### Q1: `==` 和 `equals()` 的区别是什么？在你的项目中哪里体现了？
`==` 比较引用地址，`equals()` 比较内容（自定义类需重写）。在本项目中，判断题和单选题的答案比较用的是 `equalsIgnoreCase()`（忽略大小写），而不是 `==`。因为答案是从前端传来的字符串对象，用 `==` 会得到错误结果。

#### Q2: `static` 关键字的作用？`PasswordUtil` 为什么用 `static final`？
- `static`：类级别变量/方法，所有实例共享，无需new即可访问
- `final`：引用不可变（或方法不可重写）
- `PasswordUtil` 中的 `BCryptPasswordEncoder` 是线程安全的，用 `static final` 确保全局唯一实例，避免重复创建（BCryptPasswordEncoder内部有固定的salt生成器和迭代器）

#### Q3: `StringBuilder` 和 `String` 拼接的区别？你的代码中哪里用了？
`String` 是不可变的，每次拼接创建新对象。`StringBuilder` 是可变的，适合循环拼接。本项目 `sortChars()` 方法中用了 `StringBuilder` 收集排序后的字符：
```java
s.chars().sorted()
    .collect(StringBuilder::new, StringBuilder::appendCodePoint, StringBuilder::append)
```

#### Q4: Java的异常体系？你项目中的 `BusinessException` 为什么继承 `RuntimeException`？
- `Throwable` → `Error`（不可恢复）/ `Exception`
- `Exception` → `RuntimeException`（非受检）/ 其他（受检异常）
- `BusinessException` 继承 `RuntimeException`：业务异常是预期内的，不需要强制调用方 try-catch，由 `GlobalExceptionHandler` 统一处理
- 如果用受检异常，每个方法都要声明 `throws`，代码侵入性太强

#### Q5: Lambda和Stream API在你的项目中怎么用的？
```java
// 多选题正确选项计数
long correctCount = dto.getOptions().stream()
    .filter(opt -> opt.getIsCorrect() != null && opt.getIsCorrect() == 1)
    .count();

// 过滤已完成的考试
List<Paper> availablePapers = papers.stream()
    .filter(p -> examRecordMapper.selectByStudentIdAndPaperId(...).isEmpty())
    .collect(Collectors.toList());

// 转换Option实体到VO
options.stream().map(o -> {
    PracticeResultVO.OptionVO ov = new PracticeResultVO.OptionVO();
    ov.setOptionLabel(o.getOptionLabel());
    ov.setOptionContent(o.getOptionContent());
    return ov;
}).collect(Collectors.toList());
```

#### Q6: Lombok 的原理是什么？`@Data` 做了什么？
Lombok 通过 **JSR 269（Pluggable Annotation Processing API）** 在编译期操作AST（抽象语法树），根据注解生成对应的字节码：
- `@Data` = `@Getter` + `@Setter` + `@ToString` + `@EqualsAndHashCode` + `@RequiredArgsConstructor`
- `@Getter`/`@Setter` 为所有字段生成get/set方法
- 注意：`Boolean isCorrect` → Lombok生成 `getIsCorrect()`，Jackson序列化为 `"isCorrect"`，不会去掉 `is` 前缀

---

### 4.2 Spring Boot 框架

#### Q1: Spring IoC（控制反转）和 DI（依赖注入）是什么？你的项目如何实现的？
- **IoC**：对象的创建和管理权由程序员反转给Spring容器
- **DI**：Spring在运行时将依赖注入到对象中
- 本项目中：`@Autowired` 注入Service到Controller，注入Mapper到Service
```java
@RestController
public class AuthController {
    @Autowired  // Spring自动注入
    private AuthService authService;
}
```
- Spring先创建所有Bean，再根据类型匹配注入依赖

#### Q2: `@Component`、`@Service`、`@Repository`、`@Controller` 的区别？
- 功能上基本等价，都会被Spring扫描并创建Bean
- 区别在于**语义**和**可能的增强**：
  - `@Component`：通用组件
  - `@Service`：业务逻辑层。Spring目前没有特殊处理，但便于AOP切面定位
  - `@Repository`：数据访问层。Spring会自动翻译JDBC异常为 `DataAccessException`
  - `@Controller`/`@RestController`：Web层。处理HTTP请求，`@RestController` = `@Controller` + `@ResponseBody`

#### Q3: Spring Bean 的作用域有哪些？你的项目中 Bean 默认是什么作用域？
- **singleton**（默认）：整个容器只有一个实例（本项目所有Bean都是单例）
- **prototype**：每次注入/获取都创建新实例
- **request/session/application**：Web环境下，分别绑定到HTTP请求/Session/ServletContext
- 注意：`BCryptPasswordEncoder` 作为单例是线程安全的；`HttpSession` 不是注入的，而是方法参数，每次请求独立

#### Q4: `@Configuration` 和 `@Component` 有什么区别？
- `@Configuration` 标识的类中 `@Bean` 方法会被CGLIB代理，多次调用返回同一个实例（单例保证）
- `@Component` 中 `@Bean` 方法是普通方法调用，每次返回新实例
- 本项目中 `WebMvcConfig` 用 `@Configuration`，因为需要注册拦截器

#### Q5: Spring Boot 的 starter 机制是什么？
starter 是一组依赖的集合描述符 + 自动配置类：
- `spring-boot-starter-web`：包含 spring-webmvc、jackson、tomcat等
- `mybatis-spring-boot-starter`：包含 mybatis、mybatis-spring、mybatis-spring-boot-autoconfigure
- 开发者不需要关心传递依赖的版本兼容性

#### Q6: Spring AOP 的原理？你的项目中是否用到了AOP？
- Spring AOP 基于**动态代理**（JDK动态代理或CGLIB）
- 本项目没有自定义切面，但使用了AOP机制的组件：
  - `@Transactional`：由 `TransactionInterceptor` 织入事务逻辑
  - `@RestControllerAdvice`：全局异常处理本质上是 `@ControllerAdvice`（AOP的Controller增强）

---

### 4.3 MyBatis / 数据库

#### Q1: MyBatis `#{}` 和 `${}` 的区别？你的项目中为什么都用 `#{}`？
- `#{}`：**预编译占位符**，生成 `?`，然后通过 `PreparedStatement` 安全设值 → 防SQL注入
- `${}`：**字符串替换**，直接拼接到SQL中 → 有SQL注入风险
- 本项目全部使用 `#{}`，包括动态表名/列名也没有场景需要使用 `${}`
- 特殊：`LIKE CONCAT('%', #{keyword}, '%')` 比 `LIKE '%${keyword}%'` 安全

#### Q2: MyBatis 的 ResultMap 和 ResultType 有什么区别？
- `resultType`：直接指定Java类，要求列名和属性名一致（或开启驼峰转换）
- `resultMap`：手动定义列-属性映射，支持复杂映射（关联查询、集合嵌套）
- 本项目使用 `resultMap`，因为SQL中使用了 `c.course_name`、`t.real_name AS teacher_name` 等JOIN字段，需要明确映射到实体类的 `courseName`/`teacherName` 属性

#### Q3: 多表关联查询（JOIN）在你的项目中怎么处理的？
```sql
-- Paper 联查 course_name 和 teacher_name
SELECT p.*, c.course_name, t.real_name AS teacher_name
FROM t_paper p
LEFT JOIN t_course c ON p.course_id = c.course_id
LEFT JOIN t_teacher t ON p.teacher_id = t.teacher_id
```
- 因为 `teacher_id` 无FK约束，用 `LEFT JOIN` 保证即使教师被删除也能查出来
- `course_name` 和 `teacher_name` 通过 `ResultMap` 映射到实体类的扩展字段

#### Q4: 数据库索引怎么设计的？哪些字段应该建索引？
基于本项目的查询模式：
- `t_question.course_id`：按课程筛选题目 → **必建索引**
- `t_question.question_type`、`t_question.difficulty`：联合查询过滤 → 考虑建复合索引
- `t_paper.course_id`、`t_paper.status`：按课程+状态查试卷 → 建复合索引
- `t_paper_question.paper_id`、`t_paper_question.question_id`：关联查询 → FK自动建索引
- 各表的 `username`：登录查询 → UNIQUE自动建索引
- 实际项目中，建索引需要权衡查询性能 vs 写入性能

#### Q5: MySQL 分页怎么实现的？你项目中的分页有什么问题？
```sql
-- 基于 LIMIT offset, size 的传统分页
SELECT * FROM t_question WHERE ... ORDER BY create_time DESC LIMIT 0, 10;
```
- 问题：`LIMIT 100000, 10` 时需要扫描100010行再丢弃前100000行，大偏移量性能差
- 改进方案：基于索引的游标分页（`WHERE id > lastId`）或使用ElasticSearch
- 本项目数据量小（教学场景），LIMIT分页足够

#### Q6: 连接池是什么？Spring Boot 默认用的是什么连接池？
- 连接池预先创建一组数据库连接供复用，避免频繁创建/销毁连接的开销
- Spring Boot 2.x 之前用 Tomcat JDBC Pool，2.x 开始默认用 **HikariCP**
- Spring Boot 4.0.7 默认使用 HikariCP（虽然本项目的 pom.xml 中没有显式声明，但 `spring-boot-starter-data-jdbc` 或直接通过JDBC starter引入）

---

### 4.4 安全与认证

#### Q1: 你的项目如何防止未登录访问？
通过 `LoginInterceptor` 拦截所有 `/api/**` 请求：
```java
HttpSession session = request.getSession(false);  // 不创建新Session
if (session == null || session.getAttribute("userId") == null) {
    response.getWriter().write("{\"code\":401,\"message\":\"未登录\"}");
    return false;  // 拦截
}
```

#### Q2: 如何防止越权操作？
两层防护：
1. **URL层**：`RoleInterceptor` 检查角色与URL前缀匹配
2. **数据层**：教师查询时强制过滤 `courseIds`（从 `t_teacher_course` 获取），学生只能查自己选课的数据
3. 业务方法中校验资源所有权（如 `examRecord.getStudentId().equals(studentId)`）

#### Q3: 为什么不使用 Spring Security？
- 项目规模小，3种角色，角色模型固定，不需要复杂的权限表达式和Filter链
- 自实现拦截器更轻量、更可控
- 前端是纯HTML+JS，不存在Spring Security的CSRF/表单登录/CORS集成问题
- 缺点：没有CSRF保护、无法做到方法级权限注解（如 `@PreAuthorize`）

#### Q4: Session Fixation（会话固定攻击）是什么？
- 攻击者先获取一个Session ID，诱导用户用此Session ID登录
- 登录后Session中有了用户信息，攻击者用同一Session ID即可冒充
- 防御：登录后 `request.getSession().invalidate()` + `request.getSession(true)` 重新生成Session
- 本项目的登录方法没有做Session重建，存在此风险

#### Q5: Cookie的安全属性有哪些？
- `HttpOnly`：禁止JS访问，防XSS窃取（本项目已启用）
- `Secure`：仅HTTPS传输，防中间人窃取（本项目开发环境未启用）
- `SameSite`：限制跨站请求携带Cookie（Strict/Lax/None），防CSRF
- `Max-Age`/`Expires`：Cookie有效期

#### Q6: BCrypt怎么防彩虹表？为什么说它是"慢哈希"？
- 防彩虹表：每个密码生成随机盐值，相同的密码产生不同的密文
- 慢哈希：BCrypt默认迭代2^10=1024轮Blowfish加密，单次验证约0.1秒。对用户可接受，但对暴力破解意味着每秒只能尝试10次
- 对比：SHA-256单次计算微秒级，GPU每秒可算数十亿次

---

### 4.5 系统设计

#### Q1: 如果题库有100万道题，随机抽10题怎么做？
- `ORDER BY RAND()` 在100万行上性能极差（全表扫描+临时表排序）
- **改进方案**：
  1. **应用层随机**：先 `SELECT COUNT(*)` 获取总数，随机生成10个offset，`LIMIT offset,1` 查10次（利用主键索引）
  2. **预生成随机数**：给每题增加随机数列，定时更新。查询时 `WHERE random_col BETWEEN ? AND ?`
  3. **ElasticSearch**：用 `random_score` 查询
  4. **分桶抽样**：预先将题目ID分入100个桶，随机选一个桶取10题

#### Q2: 如果同时有1000个学生参加考试，系统怎么支撑？
1. **数据库连接池**：配置足够的连接数（如HikariCP的 `maximumPoolSize`），防止连接耗尽
2. **读写分离**：考试题目读取走从库，提交写主库
3. **缓存**：试卷题目信息用Redis缓存，减少DB查询
4. **异步提交**：提交答案时先入消息队列（如RabbitMQ），异步落库+判分
5. **限流**：网关层（Nginx/Spring Cloud Gateway）限制QPS
6. **考试倒计时**：前端本地倒计时，定时与服务器同步

#### Q3: 你的项目怎么处理高并发下的库存/名额问题？
不适用。本项目没有"名额限制"场景。但如果要加入"限制考试人数"功能：
- 用Redis的 `DECR` 原子操作扣减名额
- 或者数据库 `UPDATE ... WHERE available > 0` 乐观锁

#### Q4: 如果让你设计一个微服务版本，怎么拆分？
```
├── gateway-service     API网关（认证、限流、路由）
├── auth-service        认证服务（登录、注册、Token管理）
├── user-service        用户管理（CRUD）
├── course-service      课程管理
├── question-service    题库服务（题目CRUD、随机抽题）
├── exam-service        考试服务（组卷、考试流程、评分）
├── practice-service    练习服务
└── notification-service 消息通知（考试提醒等）
```
通信方式：同步用HTTP/REST，异步用消息队列。服务发现：Nacos/Eureka。

#### Q5: 你的项目中的"软删除"是怎么体现的？
- Teacher和Student的 `status` 字段：0=禁用，1=启用。禁用后不能登录，但数据保留
- Paper的 `status` 字段：0=未发布，1=已发布，2=已回收。回收后学生不可再参加，但已有成绩保留
- 不是传统意义的 `deleted_at IS NULL` 软删除，而是业务状态管理

---

### 4.6 项目实战

#### Q1: 你在这个项目中遇到的最大挑战是什么？

> **参考回答思路**（根据实际经历选择1-2个）：

**挑战1：FK约束导致的题目删除失败**
- 问题：删除题目时MySQL报FK约束错误
- 原因：`t_question` 被4张表引用（`t_paper_question`、`t_practice_record`、`t_exam_answer`、`t_question_option`）
- 解决：在Service层按正确顺序先删关联表，再加 `@Transactional` 保证原子性
- 收获：深入理解了MySQL外键约束机制和Spring事务管理

**挑战2：判断题没有选项导致前端渲染空白**
- 问题：判断题（type=3）的 `t_question_option` 表中没有记录，前端模板渲染选项时显示空白
- 解决：在 `PracticeServiceImpl.generatePractice()` 和 `ExamServiceImpl.getExamQuestions()` 中自动生成默认"对"/"错"选项
- 收获：理解了"数据完整性"和"展示需求"之间的矛盾，以及服务端兜底的重要性

**挑战3：多选题评分顺序问题**
- 问题：学生选"B,A"，答案存储为"A,B"，直接比较返回错误
- 解决：实现 `sortChars()` 方法对两端都排序后再比较
- 收获：从业务角度考虑评分逻辑，而非纯技术实现

**挑战4：前后端字段名不匹配**
- 问题：前端用 `opt.label` 取选项标号，但后端JSON字段是 `optionLabel`
- 结果：选项内容全部为空字符串
- 解决：逐一检查实体类字段名，统一前后端命名
- 收获：严格遵循后端DTO字段名，团队协作中API文档的重要性

#### Q2: 你项目的统一响应格式 `Result<T>` 为什么用泛型？JSON序列化时泛型信息会丢失吗？
- 泛型确保编译期类型安全，`Result<LoginUserVO>` 明确了data字段的类型
- Jackson序列化时会保留具体的JSON结构（`data`字段中填充具体对象的属性）
- 但Java运行时的**泛型擦除**确实会导致反序列化时类型信息丢失：
  ```java
  Result<LoginUserVO> result = objectMapper.readValue(json,
      new TypeReference<Result<LoginUserVO>>() {});  // 需要TypeReference传递泛型信息
  ```

#### Q3: 你的项目为什么把前端HTML放在Spring Boot的 `static` 目录？
- 简化部署：只有一个JAR包，无需Nginx或Node.js服务器
- 开发阶段方便：不需要跨域代理
- 缺点：前后端耦合，不支持前端独立部署和CDN加速；修改HTML需要重新打包部署
- 适合小规模项目/内部系统，不适合大型前后端分离项目

#### Q4: 如果让你重构这个项目，你会怎么改进？
1. **前后端分离**：前端用Vue/React，独立部署，API网关做反向代理
2. **引入Spring Security**：统一认证授权，支持JWT（便于后续微服务化）
3. **加Redis**：Session集中存储（支持多实例部署）+ 热点数据缓存
4. **接口文档**：引入Swagger/OpenAPI自动生成API文档
5. **单元测试**：目前没有测试，补充Service层的JUnit + Mockito测试
6. **数据库优化**：建复合索引，慢查询监控
7. **Docker化**：编写Dockerfile和docker-compose，一键部署
8. **安全性增强**：开启CSRF防护、Secure Cookie、登录失败锁定

#### Q5: 你的项目中有没有什么已知的bug或技术债？
诚实列举1-2个（来自CLAUDE.md中的记录）：
- 登录后没有重建Session（Session Fixation风险）
- Cookie未设置Secure标记（开发环境未启用HTTPS）
- `ORDER BY RAND()` 在大数据量下性能差
- 部分方法缺少参数校验
- 没有单元测试

---

### 4.7 前端

#### Q1: 你的项目为什么选择原生JS而不是Vue/React？
- 需要快速交付，团队可能不熟悉现代框架
- 总共13个页面，每个页面功能相对独立，不需要组件复用
- 配合Tailwind CSS CDN可以快速开发界面
- 避免引入Webpack/Vite等构建工具，降低复杂度
- 代价：代码复用度低（如翻页逻辑、Modal管理等重复编写）

#### Q2: 前后端分离 vs 不分离的优缺点？
| | 不分离（本项目） | 分离 |
|---|---|---|
| 部署 | 一个JAR | 前端静态资源+后端JAR |
| 开发 | 修改HTML需重启 | 前后端独立开发 |
| 跨域 | 无 | 需配置CORS或代理 |
| 首屏速度 | 快（无异步加载框架） | 需加载JS Bundle |
| 代码复用 | 低 | 组件化复用 |
| 适用场景 | 内部系统/小项目 | 中大型项目 |

#### Q3: `credentials: 'include'` 是什么？你的项目为什么每个fetch都要加？
- Fetch API默认不携带Cookie（即使同源请求在某些场景下也不会）
- `credentials: 'include'` 强制请求携带Cookie
- 本项目使用Session认证，必须携带Cookie给后端识别用户
- 如果遗漏，后端拿不到Session → 返回401 → 跳转登录页

---

## 5. 高频追问清单

以下是面试官看完你的项目描述后，最可能追问的20个问题：

1. **"你说用了BCrypt，能讲讲加密过程和为什么安全吗？"**
2. **"外键约束的几种删除策略，你为什么有些地方不加外键？"**
3. **"如果数据库宕机了，你的Spring Boot应用会怎样？"**
4. **"`@Transactional` 在一个Service方法内部调用另一个 `@Transactional` 方法，有几个事务？"**
5. **"你能画出用户登录的完整时序图吗？从输入用户名密码到看到页面。"**
6. **"你们的多选题评分逻辑如果遇到中英文逗号混用怎么办？"**
7. **"Session 存在服务器内存中，如果重启服务会怎样？怎么解决？"**
8. **"MyBatis 一级缓存和二级缓存的区别？你的项目用了缓存吗？"**
9. **"CORS是什么？你的项目怎么处理的？为什么允许所有来源？"**
10. **"如果你要加一个'查看同班同学平均分'的功能，怎么实现？"**
11. **"Spring Boot 的启动过程，从 `main` 方法到可以接收请求，发生了什么？"**
12. **"MySQL 的 `ORDER BY RAND()` 有什么问题？50万数据随机取10条怎么做？"**
13. **"HTTP状态码 301/302/304/401/403/500 分别是什么？你项目里用了哪些？"**
14. **"RESTful 有哪些约束？你的API严格遵循了吗？"**
15. **"你的项目怎么保证同一学生不能同时考两次？"**
16. **"如果两个老师同时编辑同一道题，会怎样？怎么避免？"**
17. **"什么是N+1查询问题？你的项目里有吗？"**
18. **"`HashMap` 和 `ConcurrentHashMap` 的区别？Spring的Bean容器用的是什么Map？"**
19. **"如何设计一个防止考试作弊的系统？"**
20. **"解释一下你项目中的CORS配置为什么用 `allowedOriginPatterns(\"*\")` 而不是 `allowedOrigins(\"*\")`？"**

---

> **使用建议**：
> 1. 先通读全文，确保每个技术点都能用自己的话解释清楚
> 2. 针对高频追问清单，逐个准备口头回答（控制在2-3分钟内）
> 3. 面试时如果被问到未准备的问题，诚实说不会，然后尝试从已知原理推理
> 4. **最重要的**：不要背答案，理解原理后能举一反三。面试官会从你的回答中追问细节。
