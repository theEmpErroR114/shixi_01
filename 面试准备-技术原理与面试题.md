# 课程习题测验系统 — 面试准备（通俗版）

> 用大白话讲清楚：这个项目用了什么技术、怎么工作的、面试官会问什么。

---

## 一、这个项目是干嘛的？

一个**在线做题系统**，三种人用：

| 角色 | 能干什么 |
|------|---------|
| **管理员** | 管理老师和学生的账号，给他们分课 |
| **老师** | 出题（选择题、判断题、填空题、简答题）、组卷子、发卷子、看学生成绩 |
| **学生** | 刷题练习、参加考试、看成绩和错题 |

**举个例子**：王老师给"Java程序设计"这门课出了100道题，挑了20道组成一张试卷，发布出去。选了这门课的学生就能看到这张卷子，点进去答题，系统自动判分。

---

## 二、用到了哪些技术？（一句话说清楚每个是干嘛的）

### 后端（服务器端）

| 技术 | 一句话解释 |
|------|-----------|
| **Java** | 写代码的语言 |
| **Spring Boot** | Java的"管家框架"——帮你自动搭好架子，你不用从零写 |
| **MyBatis** | "翻译官"——帮你把Java代码翻译成SQL语句去查数据库 |
| **MySQL** | 存数据的地方（用户、题目、成绩等全在里面） |
| **BCrypt** | 密码加密工具——"123456"存进去变成一串乱码，黑客偷了也没用 |
| **Lombok** | 省事工具——自动帮你生成get/set方法，少写一堆重复代码 |
| **Maven** | "打包工具"——把代码打成可运行的jar包 |

### 前端（浏览器端）

| 技术 | 一句话解释 |
|------|-----------|
| **HTML + CSS** | 页面的骨架和样子 |
| **原生JavaScript** | 页面上的交互逻辑，没有用Vue/React框架，纯手写 |
| **Tailwind CSS** | 现成的样式库，不用自己写CSS，直接在标签上加class就行 |
| **Iconify Icon** | 图标库，显示各种小图标 |

### 部署方式

打个jar包，`java -jar` 一条命令就跑起来了，前后端都在一个包里，端口8080。

---

## 三、核心技术——用大白话讲清楚

### 3.1 Spring Boot 是什么？怎么就"自动配置"了？

**打个比方**：以前用Java写Web项目就像自己装修毛坯房——水电、墙面、地板全要自己弄。Spring Boot 就像是**精装修房**——拎包入住，水电WiFi全给你搞好了。

**"自动配置"是什么意思？**

你引个包（比如`spring-boot-starter-web`），它就自动帮你：
- 启动一个内嵌的Tomcat服务器（不用自己装）
- 配置好JSON转换（Java对象自动变成JSON返回给前端）
- 搭好请求分发的架子（哪个URL交给哪个方法处理）

你只需要写你自己的业务代码就行了。

**项目里体现在哪？**

- `pom.xml`里引入`spring-boot-starter-web` → 自动有了Web服务器
- `application.yml`里写上数据库地址账号密码 → 自动连上数据库
- `@MapperScan("com.examsystem.mapper")` → 自动扫描所有数据库操作接口

**面试可能问**：Spring Boot怎么做到自动配置的？

> **回答**：它有一个`@EnableAutoConfiguration`注解，启动时会去读一个叫`spring.factories`的文件，里面列了几百个"自动配置类"。每个配置类上都有`@ConditionalOnClass`之类的条件注解，意思是"如果项目里引了某个包，就激活这个配置"。比如你引了`mybatis-spring-boot-starter`，它检测到MyBatis的包在，就自动帮你创建SqlSessionFactory。

---

### 3.2 MyBatis 是什么？怎么把Java代码变成SQL？

**打个比方**：MyBatis 就是一个**翻译官**。你写Java接口方法，他帮你翻译成SQL语句去执行。

**工作流程**：
```
你调用 questionMapper.selectById(1)
         ↓
MyBatis 找到 QuestionMapper.xml 里同名的那条SQL
         ↓
执行 SELECT * FROM t_question WHERE question_id = 1
         ↓
把查出来的结果自动装到一个 Question 对象里
         ↓
返回给你
```

**本项目里的亮点——动态SQL**：

```xml
<select id="findByFilters">
    SELECT * FROM t_question
    WHERE 1=1
    <if test="courseId != null">AND course_id = #{courseId}</if>      ← 如果传了课程ID就加上这个条件
    <if test="questionType != null">AND question_type = #{questionType}</if>  ← 如果传了题型就加上
    <if test="keyword != ''">AND content LIKE '%关键字%'</if>           ← 如果传了关键字就模糊搜索
</select>
```

一句话：**传了什么参数就加什么条件，不传就不加**。一条SQL搞定多种筛选组合。

**配置里一个关键设置**：
```yaml
map-underscore-to-camel-case: true  # question_type → questionType
```
数据库列名是下划线风格，Java是驼峰风格，这行配置让MyBatis自动转换。

**面试可能问**：`#{}` 和 `${}` 有什么区别？为什么你全用 `#{}`？

> **回答**：`#{}` 是**预编译**，相当于SQL里写 `?` 占位符，然后安全填值。`${}` 是**直接拼字符串**到SQL里，有SQL注入风险。比如用户输入 `' OR 1=1 --`，用`${}`就会变成 `WHERE username='' OR 1=1 --'`，直接绕过了密码验证。我的项目里全部用的`#{}`。

---

### 3.3 用户登录是怎么实现的？（Session + Cookie）

**打个比方**：你去游乐园，进门买票后手上盖个章（Cookie），之后玩任何项目只要亮一下手背就行（请求携带着Cookie），工作人员看章就知道你买过票了（服务器查Session）。

**具体流程**：

```
第1步：登录
你输入 admin / 123456 → 服务器验证通过 → 服务器在内存里记一笔"这个人是管理员"
→ 返回给你一个"编号纸条"（Cookie，名字叫 QUIZ_SESSION）

第2步：之后访问任何页面
浏览器自动把这个"编号纸条"带过去 → 服务器一看编号 → "哦你是管理员，可以访问"

第3步：退出登录
服务器把自己内存里那笔记录删了 → 下次再来，"编号纸条"无效了
```

**项目里关键的配置文件**：
```yaml
server:
  servlet:
    session:
      timeout: 30m          # 30分钟不操作就自动过期
      cookie:
        name: QUIZ_SESSION  # 自定义名字，不暴露默认的JSESSIONID
        http-only: true     # JS代码读不到这个Cookie，黑客用XSS攻击偷不走
```

**面试可能问**：Session和JWT有什么区别？

> **回答**：Session是把用户信息存**服务器内存**里，只给客户端一个编号。JWT是把用户信息加密后直接存**客户端**，每次请求带上，服务器不用存。Session好处是服务器可以随时踢人（删掉Session就行），JWT好处是服务器不用存东西、适合多台服务器。我这个项目只有一台服务器，用Session最省事。

---

### 3.4 拦截器是什么？怎么拦住没登录的人？

**打个比方**：就像写字楼的前台保安。你想进任何办公室（访问API），都得先过两道关卡。

```
你访问 /api/teacher/questions
    ↓
第一关：【登录拦截器】 看你有没有"已登录"的记录
    没登录 → 直接挡回去，返回"401 请先登录"
    已登录 → 放行
    ↓
第二关：【角色拦截器】 看你是不是老师角色
    不是老师 → 挡回去，返回"403 权限不足"
    是老师 → 放行
    ↓
终于到达目的地：【Controller】执行你的请求
```

**代码长这样**：
```java
// LoginInterceptor: 检查是否登录
HttpSession session = request.getSession(false);  // false = 不创建新Session
if (session == null || session里没有userId) {
    response.getWriter().write("{\"code\":401,\"message\":\"未登录\"}");
    return false;  // ← 挡住了
}
```

**面试可能问**：拦截器和过滤器有什么区别？

> **回答**：过滤器是Servlet层面的，比拦截器更早执行，可以拦截所有请求（包括静态资源）。拦截器是Spring MVC层面的，只能拦截进到Controller的请求。简单说：**过滤器是外围墙，拦截器是内部门禁**。我这个项目用拦截器就够了，因为我只需要对`/api/`开头的请求做检查。

---

### 3.5 权限是怎么控制的？（三层防护）

**打个比方**：
- 第一层：学生不能进教师办公室（URL拦截）
- 第二层：即使进来了，也只能看到跟自己选课有关的数据（数据过滤）
- 第三层：自己的考试记录只有自己能看（所有权验证）

**第一层——URL路径拦截**：
```
/api/admin/*   → 只允许 role=admin
/api/teacher/* → 只允许 role=teacher  
/api/student/* → 只允许 role=student
```

**第二层——数据过滤（重点）**：

老师只能看到自己教的那门课的题目。怎么做到的？
```java
// 查这个老师教哪些课
List<Long> courseIds = teacherCourseMapper.selectCourseIdsByTeacherId(teacherId);
// 查题目时只查这些课里的
questionService.listQuestions(..., courseIds);  // ← 强制传courseIds
```

**为什么题目表里有个teacher_id字段却不加外键？**

这是有意设计的。题目是归属于**课程**的，不是归属于老师的。如果王老师走了，他出的题还在"Java程序设计"这门课里，新来的张老师还能接着用。所以teacher_id只是个"谁出的"标记，**不做权限过滤用**。

**面试可能问**：如果A老师和B老师教同一门课，删掉A会影响B吗？

> **回答**：不影响。第一，题目是按课程来过滤的，不是按老师。第二，teacher_id没有外键约束，删老师不会牵连题目。第三，课程-教师关联表只删掉A的那条关联记录，B的关联还在。

---

### 3.6 报错了怎么办？（全局异常处理）

**打个比方**：餐厅里统一配了服务员，不管哪个厨师出错，服务员都用标准话术回复客人，而不是让客人看到厨房一片狼藉。

```java
@RestControllerAdvice  // ← "统一服务员"
public class GlobalExceptionHandler {

    // 预期内的错误（比如"密码错误"）
    @ExceptionHandler(BusinessException.class)
    public Result handleBusiness(BusinessException e) {
        return Result.error(e.getCode(), e.getMessage());  
        // → {"code":400, "message":"密码错误", "data":null}
    }

    // 意料之外的错误（比如数据库挂了）
    @ExceptionHandler(Exception.class)
    public Result handleUnknown(Exception e) {
        log.error("出大事了", e);  // 记日志给程序员看
        return Result.error(500, "服务器内部错误");  // 给用户看模糊提示，不暴露细节
    }
}
```

**好处**：
- 不需要每个Controller里写try-catch
- 返回格式永远统一（`{code, message, data}`）
- 系统报错不会把数据库表名、SQL语句暴露给前端

---

### 3.7 密码加密——BCrypt是什么？

**打个比方**：把"123456"变成一串乱码存进数据库。即使数据库被人偷了，小偷也反推不出原始密码。

**关键原理**：

1. **加盐（Salt）**：每个密码随机掺一把"盐"（随机字符串），然后再加密。这样即使两个人密码相同，加密结果也不同。
   ```
   张三 123456 + 随机盐abc → $2a$10$abc...X7dF3k...
   李四 123456 + 随机盐xyz → $2a$10$xyz...M9pL2w...
   ```
   密文完全不同！

2. **慢加密**：BCrypt故意算得慢（约0.1秒一次）。对正常用户这点延迟无感觉，但对黑客来说——本来是每秒猜10亿个密码，现在只能猜10个，暴力破解几乎不可能。

3. **盐存在密文里**：`$2a$10$<22位盐><31位密文>`，验证时从密文中提取盐值重新算一遍即可，不用单独存盐。

**项目里怎么用的**：
```java
// 注册/重置密码时：明文 → 密文
String encoded = PasswordUtil.encode("123456");
// 结果类似：$2a$10$N9qo8uLOickgx2ZMRZoMyeIjZAgcfl7p92G1FyOqPp...

// 登录时：比对明文和密文
PasswordUtil.matches("123456", encoded);  // → true
PasswordUtil.matches("654321", encoded);  // → false
```

**面试可能问**：MD5/SHA-256为什么不适合存密码？

> **回答**：因为太快了。MD5一次计算只需要几微秒，黑客用GPU一秒可以算上百亿次。而且所有人都用同一个算法，彩虹表（预先算好的常见密码→密文对照表）可以直接反查。BCrypt慢且每个密码独立加盐，这两个问题都解决了。

---

### 3.8 事务——"要么全做，要么全不做"

**打个比方**：你给朋友转账100块——"你的账户扣100"和"朋友账户加100"必须同时成功或同时失败。不能出现"你钱扣了但朋友没收到"。

**项目里的场景——删除一道题目**：

删除题目时，跟这道题相关的数据存在**4张表**里。必须先删完这4个地方的引用，最后才能删题目本身。

```java
@Transactional  // ← "@事务"，这个方法内的SQL操作要么全部成功，要么全部回滚
public void deleteQuestion(Long questionId) {
    paperQuestionMapper.deleteByQuestionId(questionId);   // 1. 删除"试卷-题目"关联
    practiceRecordMapper.deleteByQuestionId(questionId);   // 2. 删除练习记录
    examAnswerMapper.deleteByQuestionId(questionId);        // 3. 删除考试答题记录
    questionOptionMapper.deleteByQuestionId(questionId);    // 4. 删除选项
    questionMapper.deleteById(questionId);                  // 5. 最后删题目
}
```

如果第4步失败了（比如数据库断连），前3步已经删掉的数据会**自动回滚**——回到什么都没删的状态。

**面试可能问**：`@Transactional` 在什么情况下会失效？

> **回答**：
> 1. **同一个类里方法互相调用**——A方法调用B方法，B上的`@Transactional`不生效。因为Spring事务靠AOP代理实现，类内部调用绕过了代理。
> 2. **方法不是public的**——AOP代理拦截不到私有方法。
> 3. **异常被catch了没抛出去**——事务只会在RuntimeException抛出时回滚，catch住不抛就不会回滚。

---

### 3.9 数据库——13张表怎么设计的？

```
3张用户表
├── 管理员表  t_admin
├── 教师表    t_teacher
└── 学生表    t_student

5张题目试卷表
├── 课程表          t_course
├── 题目表          t_question        ← 存题目正文和答案
├── 题目选项表       t_question_option  ← 选择题的ABCD选项
├── 试卷表          t_paper            ← 试卷名称、分值、时长
└── 试卷-题目关联表   t_paper_question   ← 一张卷子有哪些题、每题几分

3张学习记录表
├── 练习记录表     t_practice_record  ← 学生每做一题记一条
├── 考试记录表     t_exam_record      ← 一次考试的主记录
└── 考试答题明细表  t_exam_answer      ← 考试里每道题的作答

2张关联表
├── 教师-课程关联表  t_teacher_course   ← 老师教哪些课
└── 学生-课程关联表  t_student_course   ← 学生选哪些课
```

**关键设计：外键怎么处理？**

| 场景 | 做法 | 原因 |
|------|------|------|
| 删除一个老师 | 题目/试卷**不动** | 题目属于课程，换个老师接着用 |
| 删除一个学生 | 练习/考试记录**不动**，student_id设为NULL | 保留历史数据 |
| 删除一道题 | 先手动清理所有关联，再删 | 有FK约束，不能直接删 |
| 删除一门课 | **直接禁止**（有题目或试卷就不让删） | 防止误删教学数据 |

**面试可能问**：外键删除策略 CASCADE、SET NULL、RESTRICT 的区别？

> **回答**：
> - `CASCADE`：级联删除——删主表自动删从表。比如删帖子自动删评论。
> - `SET NULL`：删主表时从表外键字段设为NULL。比如学生删了，他的考试记录还在，只是student_id变成NULL。
> - `RESTRICT`（默认）：有从表数据就不让删主表。比如有试卷引用了一道题，就不让直接删题，必须先清理试卷关联。

---

### 3.10 自动判分是怎么做的？

**4种题型的判分逻辑**：

```
单选题（type=1）：学生选"A"，答案是"A" → 对
判断题（type=3）：学生选"对"，答案是"对" → 对
   ↓ 精确匹配（去掉空格、忽略大小写）

多选题（type=2）：学生选"B,A"，答案是"A,B"
   → 两边都排序 → 都变成"A,B" → 对！
   核心逻辑：排序后比较，所以选的顺序不影响结果

填空题/简答题（type=4/5）：精确匹配
   → 实际上这些题都是老师人工看，系统只做记录
```

**代码核心就是这行**：
```java
case 2: // 多选题
    return sortChars("B,A") → "AB"
        .equals(sortChars("A,B") → "AB");   // true!
```

---

### 3.11 前后端怎么传数据的？

**统一格式**——不管对错，返回格式都一样：
```json
{
    "code": 200,        // 200=成功 400=参数错 401=没登录 403=没权限 500=服务器崩了
    "message": "success",
    "data": { ... }     // 真正有用的数据
}
```

前端收到后先看 `code`：是200就处理data，不是就弹错误提示。

**分页数据长这样**：
```json
{
    "code": 200,
    "data": {
        "list": [题目1, 题目2, ...],
        "total": 50,        // 总共50题
        "page": 1,          // 当前第1页
        "pageSize": 10      // 每页10条
    }
}
```

**URL设计的套路**：
```
GET    /api/admin/teachers         ← 查列表
GET    /api/admin/teachers/5       ← 查ID=5的详情
POST   /api/admin/teachers         ← 新建
PUT    /api/admin/teachers/5       ← 修改
DELETE /api/admin/teachers/5       ← 删除
```

同一个URL，不同的HTTP方法（GET/POST/PUT/DELETE），配合不同的操作。这叫RESTful风格。

---

## 四、面试官会问什么？

### Java基础

**Q1: `==` 和 `equals()` 的区别？**

> **大白话**：`==` 比的是"是不是同一个人"（内存地址），`equals()` 比的是"长得像不像"（内容）。比如两张面值100的人民币，你用不同钱包装着，`==`会说"不是同一张"，`equals()`会说"都是100块"。我项目里字符串比较全用`equalsIgnoreCase()`，因为答案是从前端传过来的不同的字符串对象。

**Q2: `static` 是什么？**

> **大白话**：`static` 就是这个东西是**属于类的**，不属于任何一个具体对象。比如`PasswordUtil.encode()`不用new一个PasswordUtil对象就能直接调用。`static final`就是既有static特性，又**不能变**。password的加密器用`static final`，因为整个程序只需要一个加密器实例。

**Q3: String和StringBuilder的区别？**

> **大白话**：String是**不可变的**——"hello" + "world" 不是把两个字符串拼起来，而是新建了一个"helloworld"字符串，原来的两个还在内存里。循环拼接100次就创建了100个临时字符串。
> StringBuilder是**可变的**——就是在同一个对象上修改，不产生垃圾。我的`sortChars()`方法里用了StringBuilder来拼接排序后的字符。

**Q4: RuntimeException和普通Exception的区别？**

> **大白话**：RuntimeException（运行时异常）不需要强制try-catch，代码写起来清爽。我的`BusinessException`（比如"密码错误"）都是抛这种，然后被全局异常处理器统一拦截，返回友好的JSON给前端。

**Q5: Lambda表达式和Stream怎么用的？**

> 以前写过滤要for循环+if判断，现在一行搞定：
> ```java
> // 统计多选题里勾了几个正确答案
> long count = options.stream()
>     .filter(opt -> opt.getIsCorrect() == 1)  // ← 筛选isCorrect=1的
>     .count();  // ← 数个数
> ```
> 从"怎么做"变成"要什么"，写法像写需求一样。

---

### Spring Boot

**Q1: IoC和DI是什么？**

> **大白话**：IoC（控制反转）= 你以前自己new对象，现在反转给Spring帮你new。DI（依赖注入）= Spring new完之后，自动把对象塞到需要它的地方。
> ```java
> @Autowired
> private AuthService authService;  // 你不用new，Spring自动帮你填好
> ```
> 好处：Controller不需要知道AuthService怎么创建的，更换实现也方便。

**Q2: @Service、@Controller、@Repository有什么区别？**

> **大白话**：功能上**几乎一样**，都是标记"这个类交给Spring管理"。区别是**语义上的**：@Controller是门面（接请求）、@Service是做事（业务逻辑）、@Repository是存取数据。分清楚层次，代码好维护。另外@Repository会自动把数据库的异常翻译成Spring的异常。

**Q3: Spring Bean默认是单例吗？**

> 是的。单例=整个程序只有一个实例。好处是省内存，不需要重复创建对象。需要注意单例的Bean里不能持有"跟请求相关"的状态（比如用户信息），否则不同用户请求会互相串。我的@Controller、@Service都是单例，但用户信息通过方法参数传入（不存成员变量里）。

**Q4: Spring Boot Starter是什么？**

> **大白话**：就是一个**大礼包**。你引一个`spring-boot-starter-web`，相当于一次引了spring-webmvc、jackson（JSON处理）、tomcat（服务器）等十几个包，而且版本都是配好的，不会冲突。不用自己到处找包、调版本。

---

### MyBatis / 数据库

**Q1: MyBatis和直接写JDBC的区别？**

> **大白话**：JDBC需要你手动连接数据库、创建Statement、遍历结果集、关闭连接……几十行代码干一件小事。MyBatis让你只写一个接口方法+一条SQL XML，其他它都帮你做了。而且自动把查出来的数据映射成Java对象。

**Q2: 数据库怎么分页？**

> ```sql
> SELECT * FROM t_question LIMIT 0, 10;   -- 第1页：跳过0条，取10条
> SELECT * FROM t_question LIMIT 10, 10;  -- 第2页：跳过10条，取10条
> ```
> 缺点：翻到第10000页时需要跳过99990条（虽然最终只要10条），大数据量下慢。小项目够用。

**Q3: LEFT JOIN 什么场景用？**

> 我查试卷详情时：
> ```sql
> SELECT p.*, c.course_name, t.real_name AS teacher_name
> FROM t_paper p
> LEFT JOIN t_course c ON p.course_id = c.course_id
> LEFT JOIN t_teacher t ON p.teacher_id = t.teacher_id
> ```
> `LEFT JOIN` 就是"以左边的表为准，右边对得上就连上，对不上就填NULL"。用LEFT JOIN是因为老师可能被删了（teacher_id没有外键约束），但试卷本身必须查得出来。如果用INNER JOIN，老师删了连试卷都查不出来了。

---

### 安全相关

**Q1: 怎么防止人没登录就访问？**

> 拦截器+Session。每个请求进来先查有没有Session（类似查身份证），没有就打回去返回"401请登录"。

**Q2: 怎么防止学生偷看别人的考试？**

> 两层：第一，学生只能访问`/api/student/**`开头的URL，教师接口他进不去。第二，查数据时校验所有权——`examRecord.getStudentId().equals(studentId)`——不是自己的记录直接报错。

**Q3: 为什么不直接用Spring Security？**

> 项目小，就三种角色，写两个拦截器几十行代码就搞定了。加上Spring Security反而引入一堆Filter链、SecurityConfig配置，杀鸡用牛刀。而且前端是纯HTML，没有表单登录、CSRF那些复杂场景。

**Q4: Cookie怎么设置比较安全？**

> 我设置了：
> - `name: QUIZ_SESSION`：不暴露默认的JSESSIONID
> - `http-only: true`：JS代码读不到这个Cookie，即使网页被注入恶意脚本也偷不走
> - 还没设的：`secure: true`（只HTTPS传输），因为开发环境用的HTTP

---

### 项目实战相关（必问！）

**Q1: 你在这个项目中遇到的最大的困难是什么？**

> 选一个你最有感触的说：

**困难1：删题目删不掉——MySQL报外键错误**

怎么发现的：点删除按钮，前端没报错但题目还在。日志一看，MySQL报"外键约束，不能删除"。

原因：题目被4张表引用着（试卷关联、练习记录、考试答案、题目选项），数据库不让直接删。

怎么解决的：
1. 找出所有引用了题目的表
2. 在Service里按顺序先删这些表的数据
3. 加上`@Transactional`，保证中间任何一步失败全部回滚
4. MyBatis XML里补上对应的delete语句

学到什么：数据库外键不是摆设，做删除一定要先查谁引用了它。

**困难2：判断题在前端显示空白**

怎么发现的：学生练习页面，单选多选都正常，判断题题干有但选项区域是空的。

原因：判断题的选项"对/错"在数据库里没有记录（因为判断题就两个固定选项，不需要存）。

怎么解决的：在Service层加载题目时加个判断——如果是判断题但数据库没选项，自动生成"对"和"错"两个默认选项。

学到什么：数据库里不存不等于前端不需要展示，服务端该兜底的时候要兜底。

**困难3：多选题"B,A"被判错**

怎么发现的：学生反映选了B和A两个选项，结果是0分。一看数据库答案存的是"A,B"。

原因：学生选的时候可能先点B再点A，后端直接拿"B,A"跟"A,B"比，当然不相等。

怎么解决的：判断多选题时，把学生答案和正确答案都**排序**后再比较。`sortChars("B,A")` → `"AB"`，`sortChars("A,B")` → `"AB"`，匹配成功。

学到什么：业务逻辑的细节很重要，评分算法要考虑用户操作习惯。

**困难4：前端字段名写错，数据全是空的**

怎么发现的：选项内容全部显示空白。

原因：后端JSON返回的字段是`optionLabel`，前端JS里写的`opt.label`，取不到值。

怎么解决的：对着后端实体类一个字段一个字段比对，全部改成一致的名字。

学到什么：前后端字段名必须严格一致。文档很重要，靠猜一定会出错。

**Q2: 如果让你重新做这个项目，你会怎么改进？（高频题！）**

> 1. **前后端分离**：前端用Vue，独立部署。现在是HTML内嵌在JAR里，改页面要重新打包。
> 2. **加单元测试**：现在没有测试，改代码全靠手工点。加上JUnit+Mockito省心很多。
> 3. **用Spring Security**：虽然现在够用，但方法是自己写的拦截器，面试时容易被质疑安不安全。
> 4. **加Redis**：Session存Redis而不是内存，这样重启服务器用户不用重新登录。热点数据（比如试卷列表）缓存起来。
> 5. **随机抽题优化**：现在是`ORDER BY RAND()`，数据量大了铁定慢。方案：取总行数→随机生成10个行号→用主键ID直接查。
> 6. **接口文档**：加Swagger，自动生成API文档给前端看。

**Q3: 你项目有哪些已知问题？（诚实加分题）**

> 1. 登录成功后没有销毁旧Session重建新的（Session Fixation隐患）
> 2. Cookie没设置Secure标记（开发环境只有HTTP）
> 3. `ORDER BY RAND()` 随机抽题，数据大时慢
> 4. 没有单元测试
> 5. 部分接口缺少输入校验

---

## 五、面试官追问清单（20个高频追问）

1. "BCrypt加密具体过程说一下，为什么别人偷了数据库也破不了密码？"
2. "为什么teacher_id不加外键？删了老师题目怎么办？"
3. "数据库突然宕机，你的服务会怎样？连接池会怎么处理？"
4. "`@Transactional`方法里调另一个`@Transactional`方法，是几个事务？"
5. "从用户输入账号密码到看到页面，完整流程画一下？"
6. "多选判分如果遇到中文逗号、英文逗号混用怎么办？"
7. "Session存在内存里，重启服务器用户全掉了，怎么解决？"
8. "MyBatis一级缓存和二级缓存的区别？你的项目有没有用到？"
9. "CORS是什么？你配置的`allowedOriginPatterns(\"*\")`安全吗？"
10. "要做一个'查看全班平均分'的功能，SQL怎么写？"
11. "Spring Boot从启动到可以接收请求，发生了什么？"
12. "50万条数据随机取10条，`ORDER BY RAND()`为什么不行？怎么优化？"
13. "200/301/302/304/401/403/404/500 这些状态码你用过哪些？"
14. "你的API符合RESTful规范吗？哪里不符合？"
15. "怎么防止学生同一份试卷考两次？"
16. "两个老师同时编辑同一道题会有什么问题？怎么解决？"
17. "N+1查询是什么？你项目里有这种情况吗？"
18. "HashMap和ConcurrentHashMap的区别？Spring的Bean容器用了哪个？"
19. "做一个防作弊的考试系统，你会怎么设计？"
20. "为什么用`allowedOriginPatterns`而不是`allowedOrigins`？"

---

## 附：面试答题套路

**当面试官问"这个技术点你怎么理解的"：**

1. **先打比方**（10秒）：用生活例子解释是什么
2. **说原理**（30秒）：怎么工作的
3. **举项目的例子**（30秒）：我这里怎么用的
4. **说坑/局限**（20秒）：遇到什么问题、有什么缺点

**当面试官问"你遇到什么困难"：**

用STAR方法：
- **S**ituation：什么场景下
- **T**ask：要解决什么问题
- **A**ction：你做了什么
- **R**esult：结果怎样、学到什么

**最忌讳的**：
- 背概念一字不差 → 显得像背的
- 答非所问 → 没听懂要先确认
- 不懂装懂 → 诚实说"这个我没深入了解，但根据我知道的……"

**最加分的**：
- 能说出"为什么选这个而不是那个" → 显得有设计思维
- 能说出局限性 → 显得思考过
- 能说出踩过的坑 → 显得有真实经验
