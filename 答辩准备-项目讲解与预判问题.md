# 课程习题测验系统 — 答辩准备

> 本文档用来准备毕业/项目答辩。**目标：让不懂技术的评委也能听懂你的项目在做什么、用了什么技术、怎么做的**。
>
> 每一节都包含「答辩时说」的话术，可以直接拿来练。

---

## 目录

- [一、用一句话介绍你的项目](#一用一句话介绍你的项目)
- [二、项目背景：为什么要做这个](#二项目背景为什么要做这个)
- [三、项目能做什么（功能演示路线）](#三项目能做什么功能演示路线)
- [四、技术栈：用了什么 + 每个是什么 + 为什么选它](#四技术栈用了什么--每个是什么--为什么选它)
- [五、系统架构：整个项目怎么运转的](#五系统架构整个项目怎么运转的)
- [六、数据库是怎么设计的](#六数据库是怎么设计的)
- [七、核心功能的技术实现（含答辩演示话术）](#七核心功能的技术实现含答辩演示话术)
- [八、安全是怎么保障的](#八安全是怎么保障的)
- [九、开发过程中遇到的问题和解决方案](#九开发过程中遇到的问题和解决方案)
- [十、项目的不足和未来改进方向](#十项目的不足和未来改进方向)
- [十一、评委最可能问的20个问题 + 回答思路](#十一评委最可能问的20个问题--回答思路)
- [十二、答辩演示准备清单](#十二答辩演示准备清单)

---

## 一、用一句话介绍你的项目

> 我做的是一个**在线教学测验系统**。老师可以在上面出题、组卷、发考试；学生可以在上面刷题练习、参加考试、看成绩。管理员负责管理老师和学生的账号。

**答辩时说（30秒版）**：

> "各位老师好，我的项目是'课程习题测验系统'。它是一个在线教学平台，有三种角色：管理员管账号、老师出题组卷、学生刷题考试。系统会自动给选择题和判断题打分，学生做完就能看到分数和错题解析。项目用Java语言开发，数据库是MySQL，网页端可以直接用浏览器打开使用。"

---

## 二、项目背景：为什么要做这个

**现实问题**：
- 老师手动出卷、印卷、收卷、改卷，费时费力
- 学生做完题不能立刻知道对错，反馈慢
- 纸质试卷不好保存，错题不好回顾
- 疫情期间线下考试不方便

**这个系统解决的问题**：
- 老师线上出题，一次出题反复用
- 学生随时随地刷题练习，做完立刻看对错
- 系统自动判分（选择题、判断题），老师不用手动改
- 所有做题记录都在线上，随时回顾错题

**答辩时说**：

> "做这个项目是因为考虑到传统考试的几个痛点：出卷印卷耗时、改卷工作量大、学生做完不能马上知道对错。这个系统让老师线上出题组卷、学生线上做题、系统自动判分，省去了很多重复工作。"

---

## 三、项目能做什么（功能演示路线）

答辩演示时，建议按这个顺序展示：

```
登录页 → 管理员 → 教师 → 学生
```

### 管理员（admin / 123456）

| 演示什么 | 怎么演示 |
|---------|---------|
| 仪表盘 | 展示老师总数、学生总数统计卡片 |
| 老师管理 | 查看老师列表，演示搜索、新增老师 |
| 学生管理 | 查看学生列表，演示给学生分配课程 |
| 课程管理 | 点击一门课，展示这门课下有哪些题目和试卷 |

### 教师（teacher_wang / 123456）

| 演示什么 | 怎么演示 |
|---------|---------|
| 仪表盘 | 展示题目数、试卷数、学生数统计 |
| 题库管理 | 展示题目列表，演示新增一道选择题（单选/多选/判断/填空/简答五种题型切换） |
| 试卷管理 | 演示新建试卷 → 从题库中选题 → 设置每题分值 → 发布试卷 |
| 学生成绩 | 查看做过的学生成绩 |

### 学生（stu_zhang / 123456）

| 演示什么 | 怎么演示 |
|---------|---------|
| 仪表盘 | 展示可选课程 |
| 练习模式 | 选课程 → 随机出题 → 选答案 → 点"确认答案" → 立刻看到对错和解析 → 点"下一题" |
| 考试模式 | 选试卷 → 开始考试 → 限时答题 → 提交 → 看到总分和每道题的对错 |
| 成绩查看 | 查看历史成绩和错题解析 |

**答辩时说**：

> "接下来我演示一下系统的四个核心流程：管理员管账号分课程、老师出题组卷、学生刷题练习、学生参加考试。"

---

## 四、技术栈：用了什么 + 每个是什么 + 为什么选它

### 4.1 整体一览

> 所有依赖和版本声明在项目根配置文件：`exam-system-backend/pom.xml`

| 分类 | 技术名称 | 一句话解释 | 为什么选它 | 项目中的位置 |
|------|---------|-----------|-----------|-------------|
| **编程语言** | Java | 一门通用的编程语言 | 课程学的就是Java，生态成熟稳定 | `src/main/java/com/examsystem/` |
| **后端框架** | Spring Boot | "精装修房"——基础设施都搭好了，只需写业务 | 主流框架，开发快，不用从零搭 | `ExamSystemApplication.java` 启动类 |
| **数据库框架** | MyBatis | "翻译官"——把Java代码翻译成SQL语句 | 灵活写SQL，比全自动框架更可控 | `mapper/*.java` + `resources/mapper/*.xml` |
| **数据库** | MySQL | 就是一堆"Excel表格"，存数据用的 | 免费、稳定、学校教学常用 | 配置在 `resources/application.yml` |
| **密码加密** | BCrypt | "单向保险箱"——密码放进去变乱码，反推不出来 | 比MD5安全得多，行业标准 | `util/PasswordUtil.java` |
| **网页样式** | Tailwind CSS | 现成的样式工具箱，在标签上直接写class就有样式 | CDN引入，不用装任何东西 | 每个 `static/*.html` 的 `<head>` 中CDN引入 |
| **网页图标** | Iconify | 免费的图标库 | 存到本地，不依赖外网，国内访问快 | `static/iconify-icon.min.js`（自托管） |
| **网页交互** | 原生JavaScript | 网页上的"动作脚本"，点击按钮、弹窗等 | 不依赖框架，13个页面手写JS够用 | 每个 `static/*.html` 的 `<script>` 标签中 |
| **构建工具** | Maven | "打包机"——把代码打成可运行文件 | Java项目标配 | `pom.xml` |

### 4.2 每个技术分别讲清楚

#### Spring Boot（后端框架）

> 启动入口：`ExamSystemApplication.java`（`@SpringBootApplication` + `@MapperScan`）
> 配置文件：`resources/application.yml`

**比喻**：就像买精装修的房子，水电、墙漆、地板都做好了，你搬家具进去就行。

**它帮我们做了什么事**：
- 自动启动一个Web服务器（Tomcat，见 `pom.xml` 中 `spring-boot-starter-web` 依赖），不用自己装
- 请求进来了自动分发给对应的处理代码（`@RestController`、`@RequestMapping` 注解，见 `controller/` 目录下所有文件）
- 把Java对象自动转换成JSON格式返回给网页（Jackson自动配置，由 `spring-boot-starter-web` 提供）

**代码对应**：项目里的 `@RestController`、`@RequestMapping` 这些注解（见 `controller/AuthController.java`、`controller/admin/AdminTeacherController.java` 等），就是在告诉Spring Boot："这个地址的请求交给我处理"。

**答辩时说**：

> "Spring Boot是Java领域最主流的一个框架。它最大的好处是'自动配置'——以前写一个Web项目要配十几处配置文件，Spring Boot开箱即用，我们只需要写业务逻辑。它内嵌了一个Web服务器，打包好一个命令就能跑起来。"

#### MyBatis（数据库框架）

> Mapper接口：`mapper/QuestionMapper.java` 等13个接口文件
> SQL映射文件：`resources/mapper/QuestionMapper.xml` 等13个XML文件
> 配置：`resources/application.yml` 中 `mybatis` 配置段 + `ExamSystemApplication.java` 上的 `@MapperScan`

**比喻**：就像一个翻译，你对着它说Java，它帮你翻译成数据库能懂的SQL语言。

**它帮我们做了什么事**：
- 把Java方法的调用变成SQL语句的执行
- 把查出来的数据库结果自动装到Java对象里（通过 `resultMap` 配置列到属性的映射）
- 支持动态拼接SQL——传了什么条件就查什么，不传就不查（`<if test="...">` 标签）

**代码对应**：`mapper/QuestionMapper.xml` 里的那些SQL语句，和 `mapper/QuestionMapper.java` 里的方法一一对应。项目中13个Mapper接口对应13个XML文件。

**答辩时说**：

> "MyBatis是一个数据库操作框架。我们写的每一个数据库操作接口，它都会自动生成对应的实现。它还有个很重要的特性叫'动态SQL'——比如查题目时，传了课程ID就按课程筛选，传了题型就按题型筛选，不传就不筛，一条语句搞定所有组合。"

#### MySQL（数据库）

> 连接配置：`resources/application.yml` 中 `spring.datasource` 配置段
> 建表脚本：`resources/db/schema.sql`（13张表的完整DDL，含外键和索引）
> 初始数据：`config/DataInitializer.java`（`CommandLineRunner` 启动时自动插入种子数据）

**比喻**：就像一个大仓库，里面有很多货架（表），每件货物（数据）都有标签（字段）。

**本项目13张表**，分成四类：
- 账号：`t_admin` 管理员表、`t_teacher` 教师表、`t_student` 学生表
- 内容：`t_course` 课程表、`t_question` 题目表、`t_question_option` 选项表、`t_paper` 试卷表、`t_paper_question` 试卷-题目关联表
- 记录：`t_practice_record` 练习记录表、`t_exam_record` 考试记录表、`t_exam_answer` 考试答题明细表
- 关联：`t_teacher_course` 教师-课程关联表、`t_student_course` 学生-课程关联表

**答辩时说**：

> "我们用MySQL存所有数据，总共设计了13张表。核心思路是：人（管理员/教师/学生）跟课程是多对多关系——一个老师可以教多门课，一个学生可以选多门课——所以用了中间表来关联。"

#### BCrypt（密码加密）

> 代码位置：`util/PasswordUtil.java`（封装了 `spring-security-crypto` 的 `BCryptPasswordEncoder`，依赖声明在 `pom.xml`）
> 调用位置：`service/impl/AuthServiceImpl.java` 的 `login()`（验证密码）和 `changePassword()`（加密新密码），`config/DataInitializer.java`（初始化种子用户密码）

**比喻**：把密码放进一个"单向保险箱"——能放进去，但即使把箱子拆了也拿不出来。验证密码时，是把你输入的密码也放进去，看两次放进去的结果是不是一样的。

**关键特性**：
- **加盐**：每个密码掺入不同的随机字符后再加密，即使两人密码相同，密文也不一样
- **故意慢**：单次验证约0.1秒，正常人无感，但黑客暴力破解一秒只能试10次

**答辩时说**：

> "密码安全方面用了BCrypt加密。它有两个核心特点：第一是'加盐'——每个密码随机掺入一段字符串再加密，所以两个人用同样的密码，存进数据库的密文完全不同；第二是'慢哈希'——它故意算得很慢，正常人登录花0.1秒没感觉，但黑客如果想暴力破解，每秒只能试10次，破解成本巨高。"

#### 前端技术（HTML + CSS + JavaScript + Tailwind CSS）

> 所有前端页面位于：`resources/static/`（13个HTML文件，由Spring Boot直接Serve）
> 图标库：`static/iconify-icon.min.js`（自托管，避免jsdelivr CDN在国内被墙）

**比喻**：HTML是房子的骨架，CSS是装修风格，JavaScript是家里的电器开关，Tailwind CSS是现成的装修材料。

**为什么没选Vue/React？**
- 总共13个页面（`login.html`、`admin_dashboard.html`、`admin_teachers.html`、`admin_students.html`、`admin_courses.html`、`teacher_dashboard.html`、`teacher_questions.html`、`teacher_exams.html`、`teacher_students.html`、`student_dashboard.html`、`student_practice.html`、`student_exam.html`、`student_results.html`），功能独立，不需要组件化
- Tailwind CSS通过CDN引入（`cdn.tailwindcss.com`），零配置
- 如果引入Vue/React需要Webpack/Vite构建工具，增加项目复杂度

**答辩时说**：

> "前端我没有用Vue或React框架，直接用的原生HTML+CSS+JavaScript。原因是项目总共13个页面，功能相对独立，不需要前端框架的组件复用能力。样式用了Tailwind CSS，它是一个工具类优先的CSS框架，直接在HTML标签上写class就有样式，开发很快。图标用的Iconify，下载到本地了，不依赖外网。"

---

## 五、系统架构：整个项目怎么运转的

### 5.1 一句话说清楚架构

```
浏览器（网页）  ←→  Spring Boot 服务器  ←→  MySQL 数据库
```

用户在浏览器上操作 → 请求发给Java服务器 → 服务器处理逻辑 → 读写数据库 → 返回结果给浏览器。

### 5.2 请求是怎么被处理的（以"查看题目列表"为例）

```
第1步 你点"题库管理"（前端页面：static/teacher_questions.html）
      浏览器发送：GET /api/teacher/questions?courseId=1&page=1

第2步 LoginInterceptor 拦截（interceptor/LoginInterceptor.java）
      检查：你的Session里有没有登录记录？
      → 没登录 → 返回"401 请先登录"
      → 已登录 → 放行

第3步 RoleInterceptor 拦截（interceptor/RoleInterceptor.java）
      检查：你的角色是不是teacher？
      → 不是 → 返回"403 权限不足"
      → 是 → 放行

第4步 TeacherQuestionController 接收（controller/teacher/TeacherQuestionController.java）
      调用 questionService.listQuestions(...)

第5步 QuestionServiceImpl 处理（service/impl/QuestionServiceImpl.java）
      调用 questionMapper.findByFiltersAndCourseIds(...)

第6步 MyBatis 执行SQL（mapper/QuestionMapper.xml 中的 findByFiltersAndCourseIds）
      SELECT * FROM t_question WHERE course_id IN (...) ORDER BY create_time DESC LIMIT 0, 10

第7步 MySQL 返回数据 → MyBatis 映射为Java对象 → 返回给Controller

第8步 Controller 包装成统一格式返回（dto/Result.java 的 success() 方法）
      {"code":200, "data": { "list": [...], "total": 50 }, "message": "success"}

第9步 浏览器收到数据 → 渲染表格 → 你看到题目列表
```

### 5.3 项目代码结构

> 所有Java代码位于：`exam-system-backend/src/main/java/com/examsystem/`
> 所有配置文件位于：`exam-system-backend/src/main/resources/`

```
exam-system-backend/
├── pom.xml                                    Maven项目定义（依赖、插件）
└── src/main/
    ├── java/com/examsystem/
    │   ├── ExamSystemApplication.java         启动类（@SpringBootApplication + @MapperScan）
    │   ├── config/
    │   │   ├── WebMvcConfig.java              CORS + 拦截器注册
    │   │   └── DataInitializer.java           种子数据初始化（CommandLineRunner）
    │   ├── interceptor/
    │   │   ├── LoginInterceptor.java          登录检查（没有Session返回401）
    │   │   └── RoleInterceptor.java           角色检查（角色不匹配返回403）
    │   ├── controller/                        接请求的
    │   │   ├── AuthController.java            登录/登出/改密码/当前用户
    │   │   ├── CourseController.java          公共课程接口
    │   │   ├── admin/                         Admin专属
    │   │   │   ├── AdminDashboardController.java
    │   │   │   ├── AdminTeacherController.java
    │   │   │   ├── AdminStudentController.java
    │   │   │   └── AdminCourseController.java
    │   │   ├── teacher/                       Teacher专属
    │   │   │   ├── TeacherDashboardController.java
    │   │   │   ├── TeacherQuestionController.java
    │   │   │   ├── TeacherPaperController.java
    │   │   │   ├── TeacherStudentController.java
    │   │   │   └── TeacherCourseController.java
    │   │   └── student/                       Student专属
    │   │       ├── StudentDashboardController.java
    │   │       ├── StudentPracticeController.java
    │   │       ├── StudentExamController.java
    │   │       ├── StudentResultController.java
    │   │       └── StudentCourseController.java
    │   ├── service/                           业务逻辑层
    │   │   ├── AuthService.java
    │   │   ├── AdminService.java
    │   │   ├── QuestionService.java
    │   │   ├── PaperService.java
    │   │   ├── PracticeService.java
    │   │   ├── ExamService.java
    │   │   └── impl/                          上述接口的实现类
    │   ├── mapper/                            13个数据库操作接口
    │   ├── entity/                            13个实体类（和DB表一一对应）
    │   ├── dto/                               数据传输对象（Result、PageResult、各种VO）
    │   ├── enums/                             枚举（RoleEnum、QuestionTypeEnum等）
    │   ├── exception/                         BusinessException + GlobalExceptionHandler
    │   └── util/                              PasswordUtil + SessionUtil
    └── resources/
        ├── application.yml                    数据库/MyBatis/Session配置
        ├── db/schema.sql                      13张表的建表DDL
        ├── mapper/                            13个MyBatis XML映射文件
        └── static/                            13个HTML前端页面 + iconify-icon.min.js
```

**答辩时说**：

> "项目的后端代码遵循标准的分层架构：Controller层接收请求、Service层处理业务逻辑、Mapper层操作数据库。这样分层的好处是职责清晰——改SQL不影响业务逻辑，改业务逻辑不影响接口。中间还有两个拦截器做安全和权限检查。"

---

## 六、数据库是怎么设计的

### 6.1 整体结构

> 数据库建表脚本：`resources/db/schema.sql`（共13张表的完整DDL）

```
13张表分四组：

第一组：人（3张）
  t_admin     管理员表
  t_teacher   教师表  
  t_student   学生表

第二组：教学内容（5张）
  t_course          课程表
  t_question        题目表（题干、答案、解析、难度）
  t_question_option 选项表（A/B/C/D、是不是正确答案）
  t_paper           试卷表（名称、总分、时长、状态）
  t_paper_question  试卷-题目关联表（这张卷子有哪几题、每题几分）

第三组：学习记录（3张）
  t_practice_record 练习记录（每做一题记一条）
  t_exam_record     考试记录（一次考试一条）
  t_exam_answer     考试答题明细（考试里每道题的作答）

第四组：关联关系（2张）
  t_teacher_course  老师教哪些课
  t_student_course  学生选哪些课
```

### 6.2 关键设计点

**题目和试卷归属于课程，不归属于老师**（设计体现于 `resources/db/schema.sql` 的建表语句）：
- 题目表里虽然存了 `teacher_id`（谁出的题），但不加外键约束（`t_question` 表只对 `course_id` 建了FK，`teacher_id` 无FK）
- 老师离职了，他出的题还在课程里，新老师可以接着用

**答辩时说**：

> "数据库有一个重要设计决策：题目和试卷是按课程归属的，不绑定到具体老师。这样如果一位老师离职了，他的题目仍然保留在课程里，其他教授同一门课的老师可以继续使用。'

---

## 七、核心功能的技术实现（含答辩演示话术）

### 7.1 题目管理——支持五种题型

**五种题型**：

| 编号 | 题型 | 答题方式 | 是否有选项 |
|------|------|---------|-----------|
| 1 | 单选题 | 选一个 | 有ABCD选项 |
| 2 | 多选题 | 选多个 | 有ABCD选项 |
| 3 | 判断题 | 选对或错 | 系统自动生成"对/错" |
| 4 | 填空题 | 打字输入 | 无选项 |
| 5 | 简答题 | 打一段文字 | 无选项 |

**代码实现要点**（前端：`static/teacher_questions.html`，后端：`service/impl/QuestionServiceImpl.java`）：
- 单选题/多选题的答案不手写，从"哪些选项勾了正确"自动生成（单选 "A"，多选 "A,C"）
- 多选题必须至少有2个正确选项（前后端都校验）
- 判断题的"对/错"选项不用存数据库，代码里自动生成（`PracticeServiceImpl.java` 和 `ExamServiceImpl.java` 中各有实现）

**答辩时说**：

> "系统支持五种题型：单选、多选、判断、填空、简答。对于选择题，老师不需要手动输入答案——只要勾选哪些选项是正确的，系统自动生成答案。比如多选题勾了A和C正确，答案就自动填'A,C'。判断题更简单，代码自动生成'对'和'错'两个选项。"

---

### 7.2 自动判分算法

> 代码位置：`service/impl/PracticeServiceImpl.java` 和 `service/impl/ExamServiceImpl.java` 中的 `checkAnswer()` 方法

**判分逻辑**：

```
单选题：学生答案 == 正确答案 → 得分（忽略大小写、去空格）
  例：学生"A"，答案"A" → ✓
  代码：studentAnswer.trim().equalsIgnoreCase(correctAnswer.trim())

判断题：和单选题一样的逻辑
  例：学生"对"，答案"对" → ✓

多选题：两端都排序后比较（sortChars方法）
  例：学生选"B,A" → 排序成"A,B"，答案"A,B" → 排序成"A,B" → ✓
  这个设计很关键：学生选答案的顺序不影响判分

填空/简答题：精确匹配
  例：学生"封装"，答案"封装" → ✓
  实际教学中，这两种题型一般都是老师人工复核
```

**答辩时说**：

> "自动判分的核心是答案比对算法。单选题和判断题直接比较忽略大小写；多选题有个小细节——学生可能先点B再点A，所以我把学生答案和正确答案都按字母排序后再比较，这样'B,A'和'A,B'也会被判为正确。这也解决了一个实际使用中的体验问题——学生选答案的顺序不影响得分。"

---

### 7.3 随机抽题功能

> 代码位置：`mapper/QuestionMapper.xml` 中的 `findRandom` 语句，由 `service/impl/PracticeServiceImpl.java` 的 `generatePractice()` 调用

**实现方式**：
```sql
SELECT * FROM t_question 
WHERE course_id = 1 AND question_type = 1 AND difficulty = 2
ORDER BY RAND()   -- 随机排序
LIMIT 10;          -- 取前10条
```
> 来源：`resources/mapper/QuestionMapper.xml` 第111-121行

**答辩时说**：

> "学生练习时，系统根据他选的课程、题型、难度，从题库中随机抽取题目。数据库用RAND()函数实现随机排序，然后取前N条。不过这个方案在数据量非常大时性能一般，后续可以优化为应用层随机抽样。"

---

### 7.4 练习模式——"确认答案→看反馈→下一题"流程

> 前端页面：`static/student_practice.html`，后端接口：`controller/student/StudentPracticeController.java` → `service/impl/PracticeServiceImpl.java`

**为什么不能选完选项直接出结果？**

练习的目的是学习，不是考试。正确流程应该是：
```
选答案 → 点"确认答案" → 看到对错和解析（停下来思考） → 点"下一题"
```

而不是点了选项立刻跳到下一题，这样学生根本不知道自己答对还是答错。

**状态机**（前端JS实现，见 `student_practice.html`）：
```
还没确认时 → 按钮显示"确认答案"（可以改答案）
确认之后   → 按钮变成"下一题"（答案锁死，显示对错和解析）
最后一题   → 按钮变成"完成练习"
```

**答辩时说**：

> "练习模式我设计了一个'确认-反馈-继续'的流程。学生选完答案不会立刻跳到下一题，而是点了'确认答案'后，先看到自己答对还是答错、正确答案是什么、解析是什么，然后再点'下一题'。这样设计是为了保证学习效果——学生有时间停下来消化错题。"

---

### 7.5 考试模式——限时答题 + 自动提交

> 前端页面：`static/student_exam.html`，后端接口：`controller/student/StudentExamController.java` → `service/impl/ExamServiceImpl.java`

**流程**：
```
学生选试卷 → 点"开始考试" → 计时开始 → 逐题作答 
→ 点"提交"（或倒计时到自动提交） → 自动判分 → 看到成绩报告
```

**防重复考试**：考过一次且已提交的试卷，不会再出现在"可用考试"列表里（`ExamServiceImpl.java` 的 `getAvailableExams()` 方法中过滤）。

**答辩时说**：

> "考试模式下，系统会记录学生的开始时间，计算倒计时。学生在考试过程中可以跳转到任意题目、修改答案。提交后系统自动判分算总分。为了避免重复考试，已交卷的试卷不会再次出现在学生的可选列表中。"

---

### 7.6 课程权限隔离

> 核心代码：`service/impl/QuestionServiceImpl.java` 的 `listQuestions(courseIds)` 重载方法、`service/impl/PaperServiceImpl.java` 的 `listPapers(courseIds)` 重载方法、`service/impl/PracticeServiceImpl.java` 的 `generatePractice()` 方法

**核心规则**：每个人只能看到跟自己课程相关的内容。

- 老师：只能看到他教的课的题目和试卷（通过 `mapper/TeacherCourseMapper.xml` 查出课程ID列表，查询时加 `WHERE course_id IN (...)` ）
- 学生：只能看到他选的课的练习和考试（通过 `mapper/StudentCourseMapper.xml` 查出课程ID列表）

**实现方式**：从关联表查出课程ID列表，数据库查询时加 `WHERE course_id IN (...)` 过滤（见 `mapper/QuestionMapper.xml` 的 `findByFiltersAndCourseIds` 和 `mapper/PaperMapper.xml` 的 `findByFiltersAndCourseIds`）。

**答辩时说**：

> "权限控制分两层：第一层是URL拦截，管理员、教师、学生分别只能访问各自的前缀路径；第二层是数据过滤，比如教师查题目时，系统先查出他教哪些课，然后只返回这些课程下的题目。这样即使有人直接猜URL，也看不到不该看的数据。"

---

## 八、安全是怎么保障的

### 8.1 登录认证（你是谁？）

> 代码位置：`controller/AuthController.java` 的 `login()` 方法，`service/impl/AuthServiceImpl.java` 的 `login()` 方法
> Session配置：`resources/application.yml` 的 `server.servlet.session` 配置段

```
登录 → 服务器验证账号密码 → 创建Session → 返回Cookie给浏览器
之后每次请求 → 浏览器自动带Cookie → 服务器查Session确认身份
```

### 8.2 权限检查（你能干什么？）

> 代码位置：`interceptor/LoginInterceptor.java`（登录检查）、`interceptor/RoleInterceptor.java`（角色检查），两者在 `config/WebMvcConfig.java` 中注册

- 没登录 → 返回401（请先登录）
- 登录了但角色不对 → 返回403（权限不足）
- 使用拦截器（Interceptor），相当于两道门禁卡

### 8.3 密码安全

> 代码位置：`util/PasswordUtil.java`（BCrypt加密工具），依赖 `spring-security-crypto` 包（见 `pom.xml`）

- BCrypt加密存储，数据库被偷了密码也不会泄露
- 每个密码独立加盐，彩虹表无效
- Cookie设了HttpOnly（`application.yml` 中 `cookie.http-only: true`），JS代码读不到，防XSS攻击

### 8.4 数据安全

> 代码位置：`service/impl/QuestionServiceImpl.java` 的 `deleteQuestion()` 方法（`@Transactional` 删除）、`resources/db/schema.sql` 的外键策略

- 删除题目时，用事务保证关联数据一起删干净，不留孤儿数据
- 删除老师/学生时保留其历史记录（题目保留、考试记录保留——体现在 `schema.sql` 中 `ON DELETE SET NULL` 策略）

**答辩时说**：

> "安全方面做了四件事：一是登录验证，没登录的人进不来；二是角色检查，学生不能访问教师功能；三是密码加密，用BCrypt算法，数据库被偷也破解不了密码；四是数据完整性，删除数据时用事务保证不会残留垃圾数据。"

---

## 九、开发过程中遇到的问题和解决方案

答辩时挑2-3个讲，重点突出"怎么发现的→为什么→怎么解决的→学到什么"。

### 问题1：删除题目失败——数据库报外键约束错误

> 涉及代码：`service/impl/QuestionServiceImpl.java` 的 `deleteQuestion()` 方法（第149-158行）、`resources/db/schema.sql` 中的外键定义

**现象**：点删除题目按钮，题目删不掉。

**原因**：题目被4张表引用（试卷关联表 `t_paper_question`、练习记录表 `t_practice_record`、考试答案表 `t_exam_answer`、题目选项表 `t_question_option`），数据库的外键约束阻止直接删除。

**解决**：在删除题目之前，先按正确顺序删除4张关联表的数据，再删题目本身。整个过程用 `@Transactional` 包起来，保证要么全删干净、要么全不删。相关mapper方法分别位于 `PaperQuestionMapper.xml`、`PracticeRecordMapper.xml`、`ExamAnswerMapper.xml`、`QuestionOptionMapper.xml`。

**答辩时说**：

> "开发时踩了一个数据库外键的坑：删题目前必须先把跟这道题相关的试卷关联、练习记录、考试答案、选项全部删掉，顺序还不能错，删错了就报错。我的解决方案是写了一个事务方法，按正确顺序逐个清理关联数据，任何一步失败都会自动回滚。这个经历让我真正理解了数据库外键约束和事务的重要性。"

### 问题2：判断题显示空白

> 涉及代码：`service/impl/PracticeServiceImpl.java` 的 `generatePractice()` 方法（第49-60行）、`service/impl/ExamServiceImpl.java` 的 `getExamQuestions()` 方法（第110-123行）

**现象**：学生做练习时，单选题多选都正常，判断题的选项区域是空的。

**原因**：单选/多选存了ABCD选项在选项表 `t_question_option` 里，但判断题只有"对""错"两个选项，没有存进数据库。代码通过 `QuestionOptionMapper.selectByQuestionId()` 去查选项表，查不到就显示空白。

**解决**：在 `PracticeServiceImpl.generatePractice()` 和 `ExamServiceImpl.getExamQuestions()` 两个方法里加判断——如果题目类型是判断题（type=3）且数据库没有选项记录，自动生成"对"和"错"两个默认选项。

**答辩时说**：

> "判断题有个细节：数据库里没存它的'对'和'错'选项，因为这两个是固定的。但前端渲染选项时是统一逻辑，直接去数据库查。结果查不到就显示空白。后来我在加载题目时加了判断：判断题没有选项就自动补上'对'和'错'。这个问题的本质是'数据库存储'和'前端展示'之间的认知不一致。"

### 问题3：多选题判分选"B,A"被判错

> 涉及代码：`service/impl/PracticeServiceImpl.java` 和 `service/impl/ExamServiceImpl.java` 中的 `checkAnswer()` 和 `sortChars()` 方法

**现象**：学生先勾B再勾A，结果是0分，但答案就是A和B。

**原因**：学生答案存为"B,A"，标准答案是"A,B"，直接字符串比较不相等。

**解决**：多选题判分时，把学生答案和正确答案都按字母排序后再比较。`sortChars()` 方法用 Java 的 `String.chars().sorted()` 实现。

**答辩时说**：

> "多选题判分一开始有个bug：如果学生先选B再选A，答案存为'B,A'，而标准答案是'A,B'，直接比较就会判错。我加了一个排序逻辑：判分时两端都按字母排序，这样'A,B'和'B,A'都能正确匹配。这个bug让我意识到，技术实现要考虑用户的操作习惯。"

---

## 十、项目的不足和未来改进方向

**答辩时诚实说，但要显得你想过这些问题**：

| 不足 | 涉及文件 | 改进方向 |
|------|---------|---------|
| 目前没有自动测试 | 整个 `service/impl/` 目录无对应测试类 | 加 JUnit + Mockito 单元测试 |
| 前端和后台打包在一起 | `static/*.html` 在JAR内 | 前后端分离，前端用Vue独立部署 |
| Session存服务器内存，重启就丢 | `application.yml` 的 `server.servlet.session` | 加Redis集中存Session |
| 随机抽题用 `ORDER BY RAND()` | `mapper/QuestionMapper.xml` 的 `findRandom` | 改用应用层随机抽样 |
| 没有接口文档 | 所有 `controller/` 目录 | 加 Swagger/OpenAPI 自动生成API文档 |
| 密码修改后没有主动失效旧Session | `controller/AuthController.java` 的 `changePassword()` | 增加Session强制失效逻辑 |
| 没有防止考试作弊的机制 | `student_exam.html` + `ExamServiceImpl.java` | 加切屏检测、IP限制等 |

**答辩时说**：

> "项目目前完成了所有核心功能，但还有很多可以优化的地方。比如测试覆盖——现在全靠手工测试，后续应该加入自动化测试。还有架构上——现在是前后端打包在一起的，如果团队变大，应该拆成前后端分离。我列了一个待改进清单，也排了优先级，如果后续继续做会按这个路线走。"

---

## 十一、评委最可能问的20个问题 + 回答思路

### 基础类

**Q1: 这个系统主要解决了什么问题？**

> 回答：解决老师和学生在教学过程中的三个痛点——老师出卷改卷费时费力、学生做题没法立刻知道对错、纸质试卷不方便保存和回顾错题。把出题、组卷、考试、判分、成绩管理全放到了线上。

**Q2: 你项目里有哪些角色？各有什么权限？**

> 回答：三种角色。管理员管理老师和学生的账号、分配课程；老师创建题目、组卷、发布考试、看学生成绩；学生刷题练习、参加考试、看成绩和错题解析。权限是分层控制的——URL层面按角色拦截，数据层面按课程过滤。

**Q3: 简要介绍一下你项目用到的技术？**

> 回答：后端用Java+Spring Boot+MyBatis（启动类 `ExamSystemApplication.java`，配置 `application.yml`，代码分层在 controller/service/mapper 三层），数据库是MySQL（`schema.sql` 定义13张表），密码加密用BCrypt（`PasswordUtil.java`），前端是HTML+原生JS+Tailwind CSS样式（`static/` 下13个页面）。所有东西打成一个jar包，一条命令就能运行。

### 技术类

**Q4: 为什么选Spring Boot？不用Spring Boot的话会怎样？**

> 回答：Spring Boot是Java做Web项目最主流的框架。不用它的话，要手动配Tomcat、配DispatcherServlet、配JSON转换器、配数据源……光配置文件就要几十个。Spring Boot把这些都自动配好了，我把时间花在业务逻辑上。

**Q5: MyBatis和直接写SQL有什么区别？**

> 回答：直接写JDBC的话，每查一次数据库要写十几行代码（建连接、预编译、设参数、遍历结果集、关连接），代码重复且容易出错。MyBatis把这些都封装了，我只需要写一个接口方法+一条SQL，它自动完成剩下的。而且它支持动态SQL——传了条件就加WHERE子句，不传就不加。

**Q6: BCrypt加密为什么安全？**

> 回答：两点。一是每个密码都加不同的"盐"（随机字符串），相同密码产生的密文也不同，彩虹表攻击没用。二是它故意算得慢——验证一次约0.1秒，正常人无感，但黑客暴力破解每秒只能试10次，成本极高。相比MD5每秒可以算上亿次，安全性根本不是一个级别。

**Q7: `@Transactional` 是什么意思？什么场景需要它？**

> 回答：它保证一个方法里的多个数据库操作"要么全成功，要么全失败"（代码见 `QuestionServiceImpl.deleteQuestion()`）。比如删除题目时，要依次删5张表的数据，如果删到第4步时数据库断了，前3步已经删掉的数据会自动回滚恢复。没有这个注解的话，可能出现数据删一半、数据库状态不一致。

**Q8: 拦截器怎么工作的？和过滤器有什么区别？**

> 回答：拦截器相当于两道"门禁"。第一道检查"你登录了吗"（LoginInterceptor），没登录直接返回401。第二道检查"你是这个角色吗"（RoleInterceptor），角色不对返回403。它们比过滤器更贴近业务——过滤器是Web容器层面的，拦截器是Spring框架层面的，能拿到具体的Controller和方法信息。

### 数据库类

**Q9: 数据库有多少张表？为什么这么设计？**

> 回答：13张表，分成四组——人（管理员/教师/学生）、教学内容（课程/题目/选项/试卷/试卷-题目关联）、学习记录（练习记录/考试记录/答题明细）、关联关系（教师-课程/学生-课程）。这样分的原因是按"人→内容→记录→关系"的维度拆分，每张表职责清晰，不冗余。

**Q10: 老师和课程是多对多关系，你是怎么设计的？**

> 回答：用一张中间表 `t_teacher_course`（DDL见 `schema.sql` 第156-163行），只有两个字段：`teacher_id` 和 `course_id`，加上唯一约束防止重复。一个老师教多门课，就在中间表里插多条记录。查询时通过 `TeacherCourseMapper.selectCourseIdsByTeacherId()`（见 `TeacherCourseMapper.xml`）拿到课程ID列表，再去课程表查详情。

**Q11: 为什么题目表的teacher_id不加外键约束？**

> 回答：因为题目属于课程，不属于老师。如果老师离职了，他出的题应该保留在课程里，供同课程的其他老师使用。加了外键的话，删老师要么删题（不好），要么设NULL（失去记录）。所以干脆不加外键，teacher_id就当一个"谁出的"标记。

### 业务类

**Q12: 五种题型在代码里怎么区分的？**

> 回答：用数字区分——1单选、2多选、3判断、4填空、5简答。代码里用switch-case判断题型，不同题型走不同的渲染逻辑（是否显示选项列表、是否显示文本输入框）和不同的判分逻辑。

**Q13: 多选题如果学生用中文逗号"、"分隔选项怎么办？**

> 回答：目前前端生成的答案是英文逗号分隔的"B,A"，所以不会有这个问题。但如果需要防御性处理，可以在后端加一个标准化步骤——把中文逗号换成英文逗号再去空格，然后再进行排序比较。

**Q14: 怎么防止学生考试作弊？**

> 回答：目前只是基础防护——已提交的试卷不会出现在可用列表里防止重考。如果要更完善的防作弊，可以加：切屏检测（离开考试页面记录一次警告）、IP限制、随机打乱题目顺序、限制考试时间段。这些都是可以后续加的功能。

### 项目类

**Q15: 做完这个项目你最大的收获是什么？**

> 回答：最大的收获是真正理解了一个完整Web项目是怎么运转的——从浏览器点一个按钮，到数据进数据库，再到结果返回页面，整个链条我都能讲清楚。还有就是踩了很多坑，比如数据库外键约束、前后端字段名不一致、事务管理等等，这些看教程学不到的。

**Q16: 如果重新做这个项目，你会怎么改进？**

> 回答：第一，前后端分离——前端用Vue，独立部署，改页面不用重新打包。第二，加单元测试和集成测试，现在全靠手动测。第三，加Redis做缓存，热点数据不用每次都查数据库。第四，接口文档用Swagger自动化，不用口头跟队友对。

**Q17: 你的代码是怎么组织的？**

> 回答：按分层的思路组织的。Controller层（接请求）、Service层（做业务）、Mapper层（查数据库），加上Entity（数据对象）、DTO（传输对象）、Interceptor（拦截器）、Exception（异常处理）、Util（工具类）。每一层职责明确，改一个地方不影响别的。

**Q18: 有没有考虑到系统被很多人同时使用？**

> 回答：目前是单服务器部署，适合学校内部使用。如果要支撑更多用户，可以加连接池配置（HikariCP默认连接数10，可以调到50-100）、加Redis缓存减少数据库压力、读写分离（查走从库写走主库）、水平扩展（多台服务器+Nginx负载均衡）。

**Q19: 项目中的难点是什么？**

> 回答：最大的难点是删除题目时的外键级联处理——题目被4张表引用（`t_paper_question`、`t_practice_record`、`t_exam_answer`、`t_question_option`），必须按正确顺序删（见 `QuestionServiceImpl.deleteQuestion()` 第150-158行），中间任何一步都不能出错。后来用`@Transactional`注解包起来，变成事务要么全成功要么全回滚，才解决了这个问题。

**Q20: 你做了什么测试？**

> 回答：坦白说目前主要是手工功能测试——用三个角色的默认账号走完所有流程。没有写自动化测试代码，这是项目的一个重要不足。如果后续有时间，会用JUnit给Service层写单元测试，用Mockito模拟Mapper层。

---

## 十二、答辩演示准备清单

### 答辩前检查

> 启动命令：`mvn clean package -DskipTests` 编译后 `java -jar exam-system-backend/target/exam-system-backend-1.0.0.jar`（见 `pom.xml` 的 `<artifactId>` 和 `<version>`）
> 启动入口页面：`http://localhost:8080/login.html`（即 `resources/static/login.html`）

- [ ] 数据库已启动（MySQL running，数据库名 `exam_system`，建表脚本 `resources/db/schema.sql` 已执行）
- [ ] `application.yml` 中数据库连接信息正确（`spring.datasource.url/username/password`）
- [ ] 项目能正常启动（`java -jar` 不出错，`DataInitializer.java` 种子数据已初始化）
- [ ] 三个默认账号都能登录（admin / teacher_wang / stu_zhang，密码都是123456，密码加密方式见 `PasswordUtil.java`）
- [ ] 每种题型都至少有一道示例题
- [ ] 至少有一张已发布的试卷供学生考试演示
- [ ] 浏览器开无痕模式（避免Session干扰，Session名为 `QUIZ_SESSION`，30分钟过期，配置见 `application.yml`）
- [ ] 用 `http://localhost:8080/login.html` 打开

### 推荐演示路线（约5-7分钟）

```
1分钟  项目背景 + 技术栈一句话介绍
  ↓
1分钟  管理员：看仪表盘 → 老师列表 → 给学生分配课程
  ↓
2分钟  教师：出题（五种题型切换） → 组卷（选题+设分值） → 发布试卷
  ↓
2分钟  学生：练习（展示"确认→反馈→下一题"流程） → 考试（限时+提交） → 看成绩
  ↓
1分钟  总结：项目价值 + 不足之处 + 改进方向
```

### 演示时的注意事项

1. **不要照着PPT念**——打开系统直接演示
2. **语速放慢**——评委可能听不懂
3. **解释每个操作在干什么**："现在我登录教师账号，点题库管理，这里能看到王老师出的所有题……"
4. **展示关键细节**：五种题型切换、多选题判分、考试成绩页面
5. **如果出bug不要慌**——可以说"这里应该是网络问题/数据问题，正常流程是……"
6. **提前清理数据**——确保演示用到的数据都在

---

> **最后建议**：答辩前对着这篇文章，用自己话把每个部分讲一遍，录音听一遍。重点是：**让不懂技术的评委也能听懂你做了什么、为什么这么做、学到了什么**。
