<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.examsystem.mapper.TeacherMapper">

    <!-- ================================================================ -->
    <!-- 教师（Teacher）实体结果映射                                            -->
    <!-- 表：t_teacher，主键：teacher_id                                     -->
    <!-- 状态：0=禁用，1=启用                                                  -->
    <!-- ================================================================ -->
    <resultMap id="BaseResultMap" type="com.examsystem.entity.Teacher">
        <id column="teacher_id" property="teacherId"/>
        <result column="username" property="username"/>
        <result column="password" property="password"/>
        <result column="real_name" property="realName"/>
        <result column="gender" property="gender"/>                   <!-- 性别：M=男，F=女 -->
        <result column="phone" property="phone"/>
        <result column="subject" property="subject"/>                 <!-- 所教学科 -->
        <result column="status" property="status"/>                   <!-- 状态：0=禁用，1=启用 -->
        <result column="create_by" property="createBy"/>              <!-- 创建者（管理员ID） -->
        <result column="create_time" property="createTime"/>
    </resultMap>

    <!-- 根据用户名查询教师，用于登录认证 -->
    <select id="findByUsername" resultMap="BaseResultMap">
        SELECT * FROM t_teacher WHERE username = #{username}
    </select>

    <!-- 根据主键查询教师 -->
    <select id="selectById" resultMap="BaseResultMap">
        SELECT * FROM t_teacher WHERE teacher_id = #{teacherId}
    </select>

    <!-- 新增教师，主键自动回填到 teacherId -->
    <insert id="insert" useGeneratedKeys="true" keyProperty="teacherId">
        INSERT INTO t_teacher (username, password, real_name, gender, phone, subject, status, create_by, create_time)
        VALUES (#{username}, #{password}, #{realName}, #{gender}, #{phone}, #{subject}, #{status}, #{createBy}, #{createTime})
    </insert>

    <!-- 动态更新教师信息：只更新传入的非空字段（不包含密码和状态） -->
    <update id="update">
        UPDATE t_teacher
        <set>
            <if test="username != null">username = #{username},</if>      <!-- 用户名 -->
            <if test="realName != null">real_name = #{realName},</if>    <!-- 真实姓名 -->
            <if test="gender != null">gender = #{gender},</if>           <!-- 性别 -->
            <if test="phone != null">phone = #{phone},</if>              <!-- 手机号 -->
            <if test="subject != null">subject = #{subject},</if>        <!-- 所教学科 -->
        </set>
        WHERE teacher_id = #{teacherId}
    </update>

    <!-- 更新教师启用/禁用状态 -->
    <update id="updateStatus">
        UPDATE t_teacher SET status = #{status} WHERE teacher_id = #{teacherId}
    </update>

    <!-- 修改教师密码 -->
    <update id="updatePassword">
        UPDATE t_teacher SET password = #{password} WHERE teacher_id = #{teacherId}
    </update>

    <!-- 根据主键删除教师（注意：题目和试卷中 teacher_id 无FK约束，删除教师不影响题目/试卷） -->
    <delete id="deleteById">
        DELETE FROM t_teacher WHERE teacher_id = #{teacherId}
    </delete>

    <!-- 分页查询教师列表，支持多条件过滤：关键字（姓名/用户名/手机号）、状态 -->
    <select id="findByKeyword" resultMap="BaseResultMap">
        SELECT * FROM t_teacher
        <where>
            <if test="keyword != null and keyword != ''">
                AND (real_name LIKE CONCAT('%', #{keyword}, '%')       <!-- 按姓名模糊搜索 -->
                     OR username LIKE CONCAT('%', #{keyword}, '%')      <!-- 按用户名模糊搜索 -->
                     OR phone LIKE CONCAT('%', #{keyword}, '%'))        <!-- 按手机号模糊搜索 -->
            </if>
            <if test="status != null">
                AND status = #{status}                                  <!-- 按状态过滤 -->
            </if>
        </where>
        ORDER BY create_time DESC
        <if test="offset != null and limit != null">
            LIMIT #{offset}, #{limit}                                   <!-- 分页参数 -->
        </if>
    </select>

    <!-- 统计符合条件的教师总数，用于分页 -->
    <select id="countByKeyword" resultType="java.lang.Long">
        SELECT COUNT(*) FROM t_teacher
        <where>
            <if test="keyword != null and keyword != ''">
                AND (real_name LIKE CONCAT('%', #{keyword}, '%')
                     OR username LIKE CONCAT('%', #{keyword}, '%')
                     OR phone LIKE CONCAT('%', #{keyword}, '%'))
            </if>
            <if test="status != null">
                AND status = #{status}
            </if>
        </where>
    </select>

</mapper>
