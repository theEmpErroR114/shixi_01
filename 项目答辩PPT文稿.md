# 课程习题测验系统 — 项目答辩 PPT 文稿

> 每页内容按 PPT 单页组织，`---` 分隔一页幻灯片。建议 16–20 页。
>
> **📌 图例说明：**
> - 🖼️ **截图** = 运行项目后从浏览器截取的实际页面
> - 📐 **示意图** = 用 draw.io / ProcessOn / PPT 形状手工绘制的架构图、流程图
> - 🗄️ **数据库图** = 用 Navicat / DataGrip / draw.io 导出的 ER 图

---

## 第 1 页 · 封面

**课程习题测验系统**

- 基于 Spring Boot 的在线教学测验平台
- 技术栈：Spring Boot 4.0 + MyBatis + MySQL 8.x
- 前端：原生 HTML/CSS/JS + Tailwind CSS
- 角色：学生 · 教师 · 管理员

> 🖼️ **插图 A**：页面右侧放一张 **登录页面截图**（`login.html`），展示三个角色 Tab 切换的效果，作为系统门面展示。

---

## 第 2 页 · 项目背景

**传统教学测验的痛点**

- 📝 纸质试卷印制成本高、批改耗时
- ⏰ 课堂测验时间受限，无法反复练习
- 📊 成绩统计手工完成，缺乏数据沉淀
- 🌐 缺乏课下自主练习的便捷渠道

**项目目标**

打造一个支持 **课程管理 → 题库建设 → 组卷发布 → 在线作答 → 自动批改 → 成绩分析** 的全流程在线测验系统。

> 🖼️ **插图 B**：页面下方放一张 **痛点 vs 解决方案对照图**。左侧列 3 个痛点图标（纸质试卷/手工批改/无数据沉淀），右侧对应 3 个解决方案（在线题库/自动批改/成绩分析），用箭头连接。

---

## 第 3 页 · 系统总体架构

```
┌────────────────────────────────────────────┐
│              前端展示层 (13个HTML页面)         │
│   Admin (4)  │  Teacher (4)  │  Student (4) │
├────────────────────────────────────────────┤
│             Spring Boot REST API            │
│   AuthController + 10个业务Controller        │
├────────────────────────────────────────────┤
│           拦截器层 (Interceptor)              │
│    LoginInterceptor  │  RoleInterceptor     │
├────────────────────────────────────────────┤
│            业务服务层 (6个Service)             │
│   Auth · Admin · Question · Paper           │
│         Exam · Practice                     │
├────────────────────────────────────────────┤
│          数据持久层 (MyBatis + 13 Mapper)     │
├────────────────────────────────────────────┤
│              MySQL 数据库 (13张表)             │
└────────────────────────────────────────────┘
```

- 分层架构：Controller → Service → Mapper → DB
- 前后端分离式部署（静态页面同服托管）
- Session + Cookie 认证体系

> 📐 **插图 C**：将上面 ASCII 分层架构图画成一张 **彩色分层架构示意图**（draw.io 或 PPT 形状）。从上到下 5 层，每层用不同颜色区分：前端层（蓝）→ Controller（绿）→ Interceptor（橙）→ Service（紫）→ Mapper（灰）→ MySQL（青）。层间用向下箭头连接。

---

## 第 4 页 · 技术选型

| 层面 | 技术 | 版本 | 选型理由 |
|------|------|------|----------|
| 后端框架 | Spring Boot | 4.0.7 | 生态成熟，自动配置 |
| ORM | MyBatis | 4.0.1 | SQL 灵活可控 |
| 数据库 | MySQL | 8.x | 关系型，事务支持 |
| 密码加密 | BCrypt | Spring Security Crypto | 单向加盐哈希 |
| 前端样式 | Tailwind CSS | CDN | 原子化CSS，快速开发 |
| 图标库 | Iconify Icon | 自托管 | CDN 国内不可用 |
| Java 版本 | Java 26 | - | 最新LTS特性 |
| 构建工具 | Maven | - | 依赖管理 |

- 未使用 Spring Security 重量级框架，采用 **拦截器 + Session** 轻量认证方案

> 📐 **插图 D**：页面下方做一个 **技术栈分层图**（也叫"技术雷达"或堆叠图），自底向上：MySQL 8.x（数据层）→ MyBatis 4.0（持久层）→ Spring Boot 4.0（业务层）→ REST API（接口层）→ HTML+Tailwind（展示层）。每个技术旁边放一个小 Logo 图标。

---

## 第 5 页 · 数据库设计总览

**13 张数据表，分 4 大类：**

**账号体系（3 张）**
`t_admin` → `t_teacher` → `t_student`

**教学内容（5 张）**
`t_course` → `t_question` → `t_question_option`
`t_paper` → `t_paper_question`

**学习记录（3 张）**
`t_practice_record` · `t_exam_record` → `t_exam_answer`

**课程关联（2 张）**
`t_teacher_course` · `t_student_course`（多对多中间表）

> 核心设计思想：课程是教学资源的组织单位，教师和学生通过关联表与课程建立多对多关系，权限过滤全部基于课程维度。

> 🗄️ **插图 E**：页面下方放一张 **四象限分类图**，将 13 张表分成 4 个色块区域：左上"账号体系"（3 张表名）、右上"教学内容"（5 张表名）、左下"学习记录"（3 张表名）、右下"课程关联"（2 张表名）。每个色块内列出具体表名。

---

## 第 6 页 · 数据库 ER 关系图

```
  t_admin ──┐
             ├──> t_teacher ──> t_teacher_course ──┐
             │                                      ├──> t_course
             └──> t_student ──> t_student_course ──┘
                                                      │
                                  ┌────────────────────┤
                                  ▼                    ▼
                            t_question          t_paper
                                  │                    │
                            t_question_option   t_paper_question
                                  │
                    ┌─────────────┼─────────────┐
                    ▼             ▼             ▼
            t_practice_record  t_exam_record  t_exam_answer
```

**关键外键关系：**
- `t_teacher.create_by` → `t_admin.admin_id`
- `t_question.course_id` → `t_course.course_id`
- `t_paper.course_id` → `t_course.course_id`
- `t_paper_question` 连接 `t_paper` 和 `t_question`
- `t_exam_record` 关联学生和试卷
- `t_exam_answer` 关联考试记录和题目

> 🗄️ **插图 F**：**整页放一张正式的 ER 图**。推荐用 Navicat/DataGrip 的逆向工程功能直接导出，或用 draw.io 手工绘制。图中要体现 13 张表、主键、外键连线（用 Crow's Foot 表示法）。核心关系用粗线标出：`t_course` → `t_question`、`t_course` → `t_paper`、`t_paper` → `t_paper_question` → `t_question`。

---

## 第 7 页 · 权限与安全设计

**双拦截器链**

```
请求 → LoginInterceptor → RoleInterceptor → Controller
         │                    │
         ▼                    ▼
    Session验证           URL前缀校验
    (除login外全拦截)     /api/admin/* → admin
                         /api/teacher/* → teacher
                         /api/student/* → student
```

**设计要点：**
- LoginInterceptor：检查 `SESSION_USER_ID`，未登录返回 401
- RoleInterceptor：基于 URL 前缀 + Session 角色做路径级权限控制
- Admin 拥有超级权限，可访问所有 `/api/*` 路径
- 密码使用 **BCrypt** 加盐哈希存储，不存明文
- Session 超时 30 分钟，Cookie 标记 HttpOnly
- CORS 放开所有来源（开发阶段）

> 📐 **插图 G**：将上面的 ASCII 拦截器链画成 **横向流程图**。左侧"请求"方框 → 中间"LoginInterceptor"菱形（判断已登录？是→通过，否→返回401）→ "RoleInterceptor"菱形（判断角色匹配？是→通过，否→返回403）→ 右侧"Controller"方框。每个组件用不同颜色。

---

## 第 8 页 · 管理员功能模块

**账号管理**
- 教师账号：增删改查、启用/禁用、重置密码
- 学生账号：增删改查、启用/禁用、重置密码

**课程与权限**
- 课程管理（CRUD）
- 为教师分配授课课程（多对多）
- 为学生分配选课课程（多对多）

**数据看板**
- 教师总数、学生总数、课程总数统计

**关键API：**
| 方法 | 接口 | 说明 |
|------|------|------|
| GET | `/api/admin/teachers` | 教师列表（分页+搜索） |
| POST | `/api/admin/teachers` | 创建教师 |
| PUT | `/api/admin/teachers/{id}/courses` | 分配课程 |
| GET | `/api/admin/students` | 学生列表 |
| DELETE | `/api/admin/teachers/{id}` | 删除教师 |
| GET | `/api/admin/dashboard/stats` | 仪表盘统计 |

> 🖼️ **插图 H**：页面右侧放 **2 张管理页面截图**，上下排列：
> 1. **教师管理页面**（`admin_teachers.html`）— 展示教师列表表格、搜索栏、操作按钮（编辑/启用/禁用/删除）
> 2. **课程分配弹窗** — 展示为教师分配课程时的多选 checkbox 弹窗

---

## 第 9 页 · 教师功能模块

**题库管理**
- 5 种题型：单选、多选、判断、填空、简答
- 题目按课程归属，教师仅看到自己授课课程的题目
- 支持按题型、难度、关键词筛选 + 分页

**试卷管理**
- 创建/编辑/删除试卷（草稿态可编辑）
- 试卷状态：未发布(0) → 已发布(1) → 已回收(2)
- 为试卷添加题目并设置分值（总分校验）
- 支持设置考试时间窗口（开始日期 + 截止日期）

**学生管理**
- 查看自己授课课程下的学生列表

**数据看板（KPI）**
- 题目数量、试卷数量（含草稿/已发布）、活跃考试、学生数

**关键API：**
| 方法 | 接口 | 说明 |
|------|------|------|
| GET | `/api/teacher/questions` | 题目列表（分页+筛选） |
| POST | `/api/teacher/questions` | 创建题目 |
| GET | `/api/teacher/papers` | 试卷列表 |
| PUT | `/api/teacher/papers/{id}/publish` | 发布试卷 |
| GET | `/api/teacher/dashboard/stats` | 仪表盘统计 |

> 🖼️ **插图 I**：页面右侧放 **2 张教师页面截图**，上下排列：
> 1. **题库管理页面**（`teacher_questions.html`）— 展示题目列表、题型/难度筛选、创建题目表单（含 5 种题型下拉选择 + 选项编辑区）
> 2. **试卷组卷页面**（`teacher_exams.html`）— 展示试卷详情、题目列表、每题分值设置

---

## 第 10 页 · 学生功能模块

**自主练习**
- 选择课程 → 配置题型/难度/数量 → 随机抽题
- **确认再提交**流程：选择答案 → 确认答案 → 查看解析 → 下一题
- 5 种题型全覆盖：单选/多选（toggle）/判断/填空（textarea）/简答（textarea）
- 练习历史记录与错题回顾

**在线测验**
- 查看可用考试列表（含时间窗口校验）
- 限时作答，支持题目间切换 + 答案暂存
- 一键交卷 → 自动批改 → 即时出分

**成绩查询**
- 考试历史记录列表
- 逐题详情：作答内容 vs 正确答案 + 解析 + 得分

**关键API：**
| 方法 | 接口 | 说明 |
|------|------|------|
| POST | `/api/student/practice/generate` | 生成练习题 |
| POST | `/api/student/practice/submit` | 提交单题答案 |
| GET | `/api/student/exams/available` | 可用考试列表 |
| POST | `/api/student/exams/{paperId}/start` | 开始考试 |
| POST | `/api/student/exams/{examRecordId}/submit` | 提交试卷 |
| GET | `/api/student/results/exams` | 成绩历史 |

> 🖼️ **插图 J**：页面右侧放 **2 张学生页面截图**，上下排列：
> 1. **练习页面**（`student_practice.html`）— 展示练习配置（选课程/题型/难度）+ 答题界面（选项按钮 + "确认答案" + 反馈解析）
> 2. **考试页面**（`student_exam.html`）— 展示考试界面（倒计时 + 题目区 + 答题卡导航）

---

## 第 11 页 · 核心业务流程 — 教师组卷发布

```
创建试卷（草稿）
  │  填写试卷名、课程、时长、总分、考试窗口
  ▼
添加题目
  │  从题库选择 → 设置每题分值 → 校验总分
  ▼
发布试卷（状态 → 已发布）
  │  校验：总分必须精确匹配 + 至少有1道题
  ▼
学生可见、可作答
  │  时间窗口校验（startDate ≤ now ≤ endDate）
  ▼
回收试卷（状态 → 已回收）
    学生不可继续作答
```

> 📐 **插图 K**：将上面的文字流程画成一张 **垂直流程图**（或泳道图），每一步配一个图标：
> ① 📝 创建试卷 → ② ➕ 添加题目 → ③ ✅ 发布试卷 → ④ 👀 学生作答 → ⑤ 🔒 回收试卷。每一步标注对应的数据库状态值（status=0/1/2），用不同颜色区分可操作状态。

---

## 第 12 页 · 核心业务流程 — 学生考试作答

```
学生进入考试列表
  │  筛选：status=1 且 时间窗口内 且 未作答过
  ▼
点击"开始考试"
  │  校验：考试尚未开始/已截止/已提交过
  │  创建 ExamRecord (status=0 进行中)
  ▼
逐题作答
  │  单选→radio  多选→checkbox  判断→radio
  │  填空/简答→textarea
  │  答案实时暂存，可任意切换题目
  ▼
提交试卷
  │  收集所有答案 → 逐题比对标准答案
  │  自动评分（客观题精确匹配，多选题排序后比对）
  │  更新 ExamRecord (status=1 已交卷)
  ▼
查看成绩报告
    总分、正确数、逐题解析
```

> 📐 **插图 L**：将上面的文字流程画成一张 **横向泳道图**，分成 3 个泳道：
> - **前端**：展示考试列表 → 点击开始 → 逐题作答 → 提交
> - **后端**：校验权限 → 创建 ExamRecord → 返回题目 → 自动批改
> - **数据库**：INSERT ExamRecord → SELECT Questions → UPDATE ExamRecord
> 泳道间用虚线箭头标注数据流向。

---

## 第 13 页 · 自动批改引擎

**评分策略（按题型）：**

| 题型 | 批改方式 | 示例 |
|------|----------|------|
| 单选 (1) | 精确匹配 | `A` == `A` ✅ |
| 多选 (2) | 排序后比对 | `B,A` → sorted → `A,B` == `A,B` ✅ |
| 判断 (3) | 精确匹配 | `对` == `对` ✅ |
| 填空 (4) | 精确匹配 | 区分大小写 |
| 简答 (5) | 精确匹配 | 返回参考答案供人工复核 |

**设计亮点：**
- 多选题答案排序后再比对，避免顺序差异导致误判
- 判断题自动生成"对/错"选项，无需教师手动录入
- 未作答的题目自动标记为错误（得分 0）

> 📐 **插图 M**：页面上方做一个 **5 列对比卡片**，每种题型一个卡片，内部包含：
> - 图标（单选☑️ / 多选☑️☑️ / 判断✓✗ / 填空✍️ / 简答📝）
> - 输入控件示意图（radio / checkbox / radio / textarea / textarea）
> - 评分算法一句话（精确匹配 / 排序后匹配 / 精确匹配 / 精确匹配 / 参考匹配）
> - 卡片配色按题型区分

---

## 第 14 页 · 核心亮点与设计决策

**1. 课程驱动的权限模型**
- 题目和试卷归属课程，而非教师
- 教师通过 `t_teacher_course` 获得课程访问权
- 学生通过 `t_student_course` 获得学习权限
- 删除教师不影响题库资源（解除关联即可）

**2. 轻量级安全方案**
- 双拦截器（Login + Role）替代 Spring Security
- 代码简洁、调试方便、学习曲线低

**3. 确认-反馈-前进的练习模式**
- 非即时提交，学生先确认答案再看解析
- 支持错题回顾（复习模式，不计时）

**4. 灵活的考试时间窗口**
- 试卷支持 `startDate` / `endDate` 可选设置
- 考试列表自动区分"可用"和"即将开始"

> 📐 **插图 N**：页面右侧做一个 **4 宫格图**，每格对应一个设计亮点：
> 1. 课程驱动权限 — 画 t_teacher_course / t_student_course 中间表示意图
> 2. 双拦截器 — 简化的请求→拦截→Controller 链路
> 3. 确认-反馈-前进 — 3 步箭头图
> 4. 考试时间窗口 — 时间轴示意（startDate → now → endDate）

---

## 第 15 页 · 数据流与状态管理

**试卷状态机：**
```
  未发布(0) ──发布──▶ 已发布(1) ──回收──▶ 已回收(2)
     │                    │
  可编辑删除          学生可作答
                    不可编辑删除
```

**考试记录状态机：**
```
  进行中(0) ──提交──▶ 已交卷(1)
     │
  可作答（切换题目、暂存答案）
```

**统一API响应格式：**
```json
{
  "code": 200,
  "data": { ... },
  "message": "success"
}
```
- 分页接口：`data.list` + `data.total` + `data.page` + `data.pageSize`
- 单实体/列表接口：`data` 直接返回对象或数组

> 📐 **插图 O**：将上面的两个状态机画成**正式的状态转移图**（draw.io 或 PPT SmartArt）：
> - **试卷状态机**：3 个圆角矩形（未发布=灰 → 已发布=绿 → 已回收=橙），箭头标注操作（发布/回收），矩形内补充"可编辑""学生可作答""只读"等权限说明
> - **考试状态机**：2 个圆角矩形（进行中=蓝 → 已交卷=绿），箭头标注"提交试卷"

---

## 第 16 页 · 前端设计

**13 个静态 HTML 页面：**

| 角色 | 页面 | 功能 |
|------|------|------|
| 公共 | `login.html` | 角色选择 + 登录 |
| Admin | `admin_dashboard.html` | 数据看板 |
| Admin | `admin_teachers.html` | 教师管理 |
| Admin | `admin_students.html` | 学生管理 |
| Admin | `admin_courses.html` | 课程管理 |
| Teacher | `teacher_dashboard.html` | KPI看板 |
| Teacher | `teacher_questions.html` | 题库管理 |
| Teacher | `teacher_exams.html` | 试卷管理 |
| Teacher | `teacher_students.html` | 学生查看 |
| Student | `student_dashboard.html` | 课程概览 |
| Student | `student_practice.html` | 自主练习 |
| Student | `student_exam.html` | 在线考试 |
| Student | `student_results.html` | 成绩查询 |

**UI 规范：** Tailwind CSS 原子样式 + Iconify 图标 + 统一头部用户下拉菜单 + 响应式布局

> 🖼️ **插图 P**：将上面 13 个页面的表格做成一张**页面导航关系图**。中心放 `login.html`，向外辐射 3 个分支（Admin/Teacher/Student），每个分支下列出其 4 个子页面，用缩略图 + 页面名称展示。或者直接放一张**所有 13 页面的缩略图拼贴墙**（用浏览器截图后缩小排列）。

---

## 第 17 页 · 项目开发中的挑战与解决

**挑战 1：多对多课程关联与权限过滤**
- 问题：教师只能看到自己授课课程的题库，学生只能练习自己选课的题目
- 方案：通过 `t_teacher_course` / `t_student_course` 中间表 + `courseIds` 参数在 SQL 层做 `IN` 过滤

**挑战 2：删除题目时的外键级联**
- 问题：`t_question` 被 4 张表通过外键引用
- 方案：在 Service 层按顺序删除关联数据（paper_question → practice_record → exam_answer → question_option → question），使用 `@Transactional` 保证原子性

**挑战 3：5 种题型的统一渲染与交互**
- 问题：不同题型需要不同的输入控件（radio/checkbox/textarea）
- 方案：前端根据 `questionType` 整数动态渲染，练习和考试页面复用相似的渲染逻辑

**挑战 4：前端 JS 代码质量**
- 问题：原生 JS 无类型检查，API 字段名不匹配导致静默失败
- 方案：总结出 30+ 条常见 Bug 模式文档（CLAUDE.md），覆盖 `.id` 陷阱、字段名不匹配、类型混淆等

> 📐 **插图 Q**：做一个 **4 宫格挑战-解决对照图**，每格内部一分为二：上半部分红底白字写"问题"，下半部分绿底白字写"方案"。4 个格子的标题分别是：① 多对多权限过滤 ② 外键级联删除 ③ 5 题型统一渲染 ④ JS 代码质量。

---

## 第 18 页 · 项目测试数据

**预置种子数据（DataInitializer 自动初始化）：**

| 账号 | 角色 | 用户名 | 密码 |
|------|------|--------|------|
| 系统管理员 | Admin | `admin` | `123456` |
| 王老师 | Teacher | `teacher_wang` | `123456` |
| 李老师 | Teacher | `teacher_li` | `123456` |
| 张三 | Student | `stu_zhang` | `123456` |
| 刘芳 | Student | `stu_liu` | `123456` |
| 陈明 | Student | `stu_chen` | `123456` |

**预置课程：** Java程序设计、数据库原理

**预置题目：** 2道选择题 + 1道判断题（含选项和解析）

**课程分配：** 王老师→Java程序设计，李老师→数据库原理；张三选两门，刘芳选Java，陈明选数据库

> 📐 **插图 R**：做一个**账号-课程关系图**。左侧列 3 个角色分组（Admin×1 / Teacher×2 / Student×3），右侧列 2 门课程（Java程序设计 / 数据库原理），中间用箭头连线表示归属关系。颜色按角色区分：Admin=红，Teacher=蓝，Student=绿。也可以直接在 MySQL 客户端查询结果截图上标注。

---

## 第 19 页 · 项目总结

**已完成功能：**
- ✅ 三角色（Admin / Teacher / Student）完整权限体系
- ✅ 课程驱动的题库管理（5 种题型）
- ✅ 试卷组卷、发布、回收全生命周期
- ✅ 学生自主练习（随机抽题 + 确认反馈 + 错题回顾）
- ✅ 在线限时考试（时间窗口 + 自动批改 + 即时出分）
- ✅ 成绩历史查询与逐题解析
- ✅ 前后端完整 13 页面 + 35+ REST API

**技术指标：**
| 指标 | 数值 |
|------|------|
| 数据库表 | 13 张 |
| REST API 端点 | 35+ |
| 前端页面 | 13 个 |
| Java 类 | 50+ |
| MyBatis Mapper | 13 个 |
| Service 接口 | 6 个 |

> 🖼️ **插图 S**：做一个**功能完成度仪表盘**。左侧放 7 个勾选框（7 项已完成功能），右侧放 **技术指标雷达图**（6 维度：数据库表 / API 端点 / 前端页面 / Java 类 / Mapper / Service）。雷达图用 PPT 图表或 ECharts 截图。

---

## 第 20 页 · 未来展望

**短期优化：**
- 📱 响应式移动端适配
- 📊 数据可视化看板（成绩分布图表、知识点掌握度分析）
- 🔔 考试提醒通知（即将开始/即将截止）
- 📄 试卷导出（PDF/Word）

**中期扩展：**
- 🧠 智能组卷（按难度比例、知识点分布自动抽题）
- 🤖 AI 辅助批改（简答题语义相似度评分）
- 📈 学习轨迹分析（个人薄弱知识点识别）
- 🔗 第三方登录集成（OAuth 2.0）

**长期规划：**
- ☁️ 微服务化拆分（用户服务、题库服务、考试服务）
- 📦 Docker 容器化部署
- 🎓 对接学校教务系统（SSO 单点登录）

> 📐 **插图 T**：做一个**三阶段演进路线图（Roadmap 时间轴）**：
> - **短期**（本学期）：4 个卡片（移动端适配 / 数据可视化 / 考试提醒 / 试卷导出）
> - **中期**（下学期）：4 个卡片（智能组卷 / AI 批改 / 学习轨迹 / OAuth）
> - **长期**（未来）：3 个卡片（微服务 / Docker / 教务对接）
> 时间轴从左到右，每个节点用不同颜色（短期=绿 / 中期=蓝 / 长期=紫）

---

## 第 21 页 · 致谢

**课程习题测验系统**

```
   ┌──────────┐    ┌──────────┐    ┌──────────┐
   │  📚 学习  │    │  📝 练习  │    │  📊 测验   │
   │  课程管理  │    │  题库建设  │    │  自动批改  │
   └──────────┘    └──────────┘    └──────────┘
```

- 技术栈：Spring Boot + MyBatis + MySQL
- 前端：HTML/CSS/JS + Tailwind CSS

**感谢聆听，欢迎提问！** 🙏

> 🖼️ **插图 U**：页面背景使用**系统三个核心页面的淡色截图拼接**（答题界面 + 题库管理 + 成绩报告），中间叠加文字。或者放一张 **QR 码**（项目 GitHub 仓库地址，如有）。

---

> ## 📋 插图制作清单（汇总）
>
> | 编号 | 页码 | 类型 | 内容 | 制作工具建议 |
> |------|------|------|------|-------------|
> | A | 1 | 🖼️ 截图 | 登录页面 | 浏览器截图 |
> | B | 2 | 📐 示意图 | 痛点 vs 解决方案对照 | PPT 形状 / Canva |
> | C | 3 | 📐 示意图 | 分层架构图（彩色） | draw.io / ProcessOn |
> | D | 4 | 📐 示意图 | 技术栈堆叠图 | PPT 形状 + 图标 |
> | E | 5 | 📐 示意图 | 13 表四象限分类 | PPT 形状 |
> | F | 6 | 🗄️ ER图 | 完整 13 表 ER 图 | Navicat 逆向工程 / draw.io |
> | G | 7 | 📐 示意图 | 双拦截器流程图 | draw.io / PPT |
> | H | 8 | 🖼️ 截图 | 教师管理 + 课程分配弹窗 | 浏览器截图 |
> | I | 9 | 🖼️ 截图 | 题库管理 + 试卷组卷 | 浏览器截图 |
> | J | 10 | 🖼️ 截图 | 练习页面 + 考试页面 | 浏览器截图 |
> | K | 11 | 📐 示意图 | 组卷发布流程图 | draw.io / ProcessOn |
> | L | 12 | 📐 示意图 | 考试作答泳道图 | draw.io（泳道模板） |
> | M | 13 | 📐 示意图 | 5 题型评分卡片组 | PPT / Canva |
> | N | 14 | 📐 示意图 | 4 大设计亮点 4 宫格 | PPT 形状 |
> | O | 15 | 📐 示意图 | 试卷 + 考试状态机 | draw.io / PPT |
> | P | 16 | 🖼️ 截图 | 13 页面导航图或缩略图墙 | 浏览器截图拼贴 |
> | Q | 17 | 📐 示意图 | 挑战-方案 4 宫格 | PPT 形状 |
> | R | 18 | 📐 示意图 | 账号-课程关系图 | PPT / draw.io |
> | S | 19 | 🖼️+📐 | 完成度清单 + 技术雷达图 | PPT 图表 / ECharts |
> | T | 20 | 📐 示意图 | 三阶段 Roadmap 时间轴 | PPT / draw.io |
> | U | 21 | 🖼️ 截图 | 页面拼贴背景 + QR 码 | 浏览器截图 + QR 生成器 |
>
> **截图总需求：约 10 张页面截图**（需启动项目后从浏览器截取）
> **制图总需求：约 14 张示意图/流程图**（推荐 draw.io 或 ProcessOn 在线绘制）

---

> **使用建议：**
> - 根据答辩时间控制在 15-20 页（合并或拆分上述页面）
> - 第 6 页 ER 图、第 7 页拦截器链、第 15 页状态机建议绘制为图示
> - 第 11-12 页业务流程建议用流程图动画展示
> - 可在答辩前准备一个现场 Demo（登录 → 教师出题组卷 → 学生考试 → 查看成绩）
