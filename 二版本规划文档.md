# 课程习题测验系统 — 二版本规划文档

> 版本：v2.0 规划 | 日期：2026-07-31 | 状态：规划中（尚未实施）

---

## 目录

- [一、规划概览](#一规划概览)
- [二、功能模块一：通知系统](#二功能模块一通知系统)
- [三、功能模块二：错题本](#三功能模块二错题本)
- [四、功能模块三：试卷导出](#四功能模块三试卷导出)
- [五、体验优化](#五体验优化)
- [六、数据库变更汇总](#六数据库变更汇总)
- [七、新增 API 接口汇总](#七新增-api-接口汇总)
- [八、实施路线图](#八实施路线图)
- [九、风险与注意事项](#九风险与注意事项)

---

## 一、规划概览

### 1.1 当前版本（v1.0）已完成功能

- **三端分离**：管理员、教师、学生，13 张数据表，47 个 API 接口
- **账号管理**：管理员创建/管理教师和学生账号，课程分配
- **题库管理**：教师创建 5 种题型（单选/多选/判断/填空/简答），支持选项和答案解析
- **试卷管理**：教师组卷、发布/回收、设置考试时间窗口
- **学生练习**：按课程/题型/难度随机生成练习，逐题确认答案并查看解析
- **学生考试**：在线考试、倒计时、自动判分、成绩查看
- **成绩统计**：教师查看学生练习/考试统计数据

### 1.2 当前版本已知问题（v1.0 技术债）

以下问题在代码审查中发现，v2.0 应一并修复：

| 问题 | 所在页面/模块 | 严重程度 |
|------|--------------|----------|
| 累计错题统计卡片硬编码为 `'-- 题'`，从未填充数据 | `student_results.html` | 🔴 |
| 考试排名列始终显示 `'--'`，后端从未计算排名 | `student_results.html` | 🔴 |
| 学生 dashboard 平均正确率/平均测验分始终显示 `'--'` | `student_dashboard.html` | 🔴 |
| 教师 dashboard "正在进行的测验"进度条始终为 0/0（后端未返回 submittedCount） | `teacher_dashboard.html` | 🔴 |
| 教师 dashboard "待批阅简答题"仅为占位符，无后端支持 | `teacher_dashboard.html` | 🟡 |
| 考试页面无断线保护，刷新/关闭标签页后答题进度全部丢失 | `student_exam.html` | 🔴 |
| 考试倒计时纯前端计算，刷新后重置 | `student_exam.html` | 🟡 |
| 试卷列表无分页（pageSize 硬编码 20，无"加载更多"），paperCount 仅反映已加载页 | `teacher_exams.html` | 🟡 |
| 教师题目管理 hover 操作按钮在触屏设备不可见（`opacity-0 group-hover:opacity-100`） | `teacher_questions.html` | 🟡 |
| 所有页面使用浏览器 `alert()` 弹窗报错，体验差 | 全部 13 个 HTML 页面 | 🟡 |
| 公共 JS 代码（apiGet/apiPost/loadCurrentUser等）在 13 个页面中大量重复 | 全部页面 | 🟡 |
| 试卷编辑时组卷选题 pageSize 硬编码 200，题库大时不可达 | `teacher_exams.html` | 🟢 |

### 1.3 二版本（v2.0）目标

v2.0 聚焦于**四个方向**：

| 序号 | 模块 | 优先级 | 说明 |
|------|------|--------|------|
| 1 | 通知系统 | 🔴 高 | 系统公告 + 考试/成绩通知，站内信方式 |
| 2 | 错题本 | 🔴 高 | 自动收集错题，支持重练和掌握标记 |
| 3 | 试卷导出 | 🟡 中 | 教师导出试卷为 Word/PDF，支持含答案/不含答案两种版本 |
| 4 | 体验优化 | 🟡 中 | 加载状态、交互反馈、数据可视化、移动端适配等 |

### 1.4 不纳入 v2.0 的功能

以下功能虽在讨论范围内，但考虑工作量和优先级，推迟到后续版本：

- **即时通讯（聊天）**：需要 WebSocket 长连接，前后端改动大，涉及消息存储、在线状态管理，建议 v3.0 再做
- **第三方通知（邮件/短信/微信）**：依赖外部服务，配置复杂，先做站内通知
- **AI 辅助出题**：需要对接 LLM API，成本和技术复杂度高

> **设计原则**：v2.0 新增功能尽量不破坏现有 API 和数据库结构，以新增表和扩展字段为主，保证向后兼容。

---

## 二、功能模块一：通知系统

### 2.1 需求说明

当前系统缺少消息通知机制：
- 管理员无法向教师/学生发布系统公告
- 教师发布试卷后，学生不知道有新考试
- 考试成绩出分后，学生不知道
- 教师无法向所教班级发送通知

v2.0 实现**站内通知**（类似站内信/消息中心），不做实时推送，采用**页面加载时拉取 + 手动刷新**的方式。

### 2.2 通知类型

| 类型 | 触发方 | 接收方 | 触发时机 |
|------|--------|--------|----------|
| 系统公告 | 管理员 | 教师 + 学生 | 管理员主动发布 |
| 考试通知 | 系统自动 | 该课程学生 | 教师发布试卷 |
| 成绩通知 | 系统自动 | 学生 | 学生提交考试后自动判分完成 |
| 试卷回收通知 | 系统自动 | 该课程学生 | 教师回收试卷 |
| 课程通知 | 教师 | 所教课程学生 | 教师主动发送（可选，后期扩展） |

### 2.3 数据库设计

新增 **1 张表**：`t_notification`

```sql
CREATE TABLE IF NOT EXISTS t_notification (
    notification_id   BIGINT AUTO_INCREMENT PRIMARY KEY COMMENT '通知ID',
    title             VARCHAR(200)  NOT NULL COMMENT '通知标题',
    content           TEXT          NOT NULL COMMENT '通知正文',
    type              TINYINT       NOT NULL COMMENT '通知类型：1系统公告 2考试通知 3成绩通知 4回收通知 5课程通知',
    sender_id         BIGINT        COMMENT '发送者ID（管理员/教师ID，系统自动通知为NULL）',
    sender_role       VARCHAR(20)   COMMENT '发送者角色：admin/teacher/system',
    target_role       VARCHAR(20)   NOT NULL COMMENT '目标角色：all/teacher/student',
    target_user_id    BIGINT        COMMENT '目标用户ID（NULL表示该角色的所有用户）',
    target_course_id  BIGINT        COMMENT '关联课程ID（NULL表示非课程相关）',
    ref_id            BIGINT        COMMENT '关联业务ID（如exam_record_id、paper_id）',
    ref_type          VARCHAR(30)   COMMENT '关联业务类型：exam/paper/practice',
    is_read           TINYINT DEFAULT 0 COMMENT '是否已读：0未读 1已读',
    is_global         TINYINT DEFAULT 0 COMMENT '是否全局通知：1全局（所有目标角色可见） 0定向通知',
    create_time       DATETIME DEFAULT CURRENT_TIMESTAMP COMMENT '创建时间',
    INDEX idx_target (target_role, target_user_id, is_read),
    INDEX idx_create_time (create_time)
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4 COMMENT='通知表';
```

**设计说明**：
- `is_global=1` 的记录只有一条，所有目标用户可见（系统公告），通过 `t_notification_read` 表跟踪每个人的阅读状态
- `is_global=0` 的记录为定向通知，每个用户一条，直接修改 `is_read`

> **方案选择**：全局通知 + 已读状态表，避免为每个用户复制一份公告内容。定向通知直接为目标用户创建记录。

新增 **1 张表**：`t_notification_read`（全局已读状态，仅全局通知使用）

```sql
CREATE TABLE IF NOT EXISTS t_notification_read (
    id              BIGINT AUTO_INCREMENT PRIMARY KEY COMMENT '关联ID',
    notification_id BIGINT NOT NULL COMMENT '通知ID',
    user_id         BIGINT NOT NULL COMMENT '用户ID',
    user_role       VARCHAR(20) NOT NULL COMMENT '用户角色',
    read_time       DATETIME DEFAULT CURRENT_TIMESTAMP COMMENT '阅读时间',
    UNIQUE KEY uk_notif_user (notification_id, user_id, user_role),
    CONSTRAINT fk_nr_notif FOREIGN KEY (notification_id) REFERENCES t_notification (notification_id)
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4 COMMENT='通知已读状态表（全局通知用）';
```

### 2.4 后端 API 设计

| 方法 | URL | 权限 | 说明 |
|------|-----|------|------|
| POST | `/api/notifications/announce` | admin | 发布系统公告 |
| GET | `/api/notifications` | 登录 | 获取当前用户的通知列表（分页，按时间倒序） |
| GET | `/api/notifications/unread-count` | 登录 | 获取未读通知数量 |
| PUT | `/api/notifications/{id}/read` | 登录 | 标记单条通知为已读 |
| PUT | `/api/notifications/read-all` | 登录 | 全部标记已读 |
| DELETE | `/api/notifications/{id}` | 登录 | 删除通知（软删除，或直接删除定向通知） |

**自动触发场景**：
- `PaperServiceImpl.publishPaper()` → 发布后为每个选修该课程的学生创建考试通知
- `ExamServiceImpl.submitExam()` → 交卷后为学生创建成绩通知
- `PaperServiceImpl.recallPaper()` → 回收后为相关学生创建回收通知

### 2.5 前端设计

**通知入口**：所有页面顶部导航栏右侧，用户头像旁边增加**通知铃铛图标** 🔔

```
┌──────────────────────────────────────────────────┐
│  Logo   导航1  导航2  ...         🔔(3) 👤 用户名  │
└──────────────────────────────────────────────────┘
```

- 铃铛右上角红色徽标显示未读数量
- 点击铃铛弹出下拉通知列表（最近 5 条，带"查看全部"链接）
- 未读通知以加粗或高亮背景显示
- 点击单条通知标记已读并跳转（如考试通知 → 跳转到考试页面）

**通知中心页面**（新建 `notifications.html`，所有角色共用）：
- 左侧按类型筛选：全部 / 系统公告 / 考试通知 / 成绩通知
- 右侧通知列表（分页），每条显示：图标 + 标题 + 时间 + 已读/未读状态
- 点击展开查看详情
- 顶部"全部标为已读"按钮

### 2.6 技术要点

- **不做 WebSocket 实时推送**，采用页面加载和定时轮询（每 60 秒检查一次未读数量）
- 通知不存储在 Session 中，持久化到数据库
- 管理员发送全局公告时，只插入一条 `t_notification` 记录（`is_global=1`），不复制
- 自动通知（考试/成绩）在业务方法内同步创建，不异步
- 通知不设过期删除机制（v2.0 不做自动清理，后续版本可加）

---

## 三、功能模块二：错题本

### 3.1 需求说明

学生在练习和考试中做错的题目，自动收录到个人错题本。学生可以：
- 查看所有错题，按课程/题型/难度筛选
- 对错题进行重新练习
- 标记错题为"已掌握"（从错题本移除或隐藏）
- 查看错题统计（各课程错题数量、薄弱知识点）

### 3.2 数据来源

错题数据**不需要新建表**，直接查询现有数据：

| 来源 | 表 | 筛选条件 |
|------|-----|----------|
| 练习错题 | `t_practice_record` | `student_id = ? AND is_correct = 0` |
| 考试错题 | `t_exam_answer` | JOIN `t_exam_record` WHERE `student_id = ? AND is_correct = 0 AND t_exam_record.status = 1` |

**去重逻辑**：同一道题可能在练习和考试中多次做错。错题本按题目去重，显示首次做错时间和错误次数。

### 3.3 数据库设计

新增 **1 张表**：`t_wrong_book`（错题本，记录掌握状态和统计信息）

```sql
CREATE TABLE IF NOT EXISTS t_wrong_book (
    id              BIGINT AUTO_INCREMENT PRIMARY KEY COMMENT '记录ID',
    student_id      BIGINT NOT NULL COMMENT '学生ID',
    question_id     BIGINT NOT NULL COMMENT '习题ID',
    wrong_count     INT DEFAULT 1 COMMENT '错误次数',
    first_wrong_time DATETIME COMMENT '首次做错时间',
    last_wrong_time  DATETIME COMMENT '最近做错时间',
    is_mastered     TINYINT DEFAULT 0 COMMENT '是否已掌握：0未掌握 1已掌握',
    mastered_time   DATETIME COMMENT '掌握时间',
    UNIQUE KEY uk_student_question (student_id, question_id),
    CONSTRAINT fk_wb_student  FOREIGN KEY (student_id)  REFERENCES t_student (student_id),
    CONSTRAINT fk_wb_question FOREIGN KEY (question_id) REFERENCES t_question (question_id)
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4 COMMENT='错题本表';
```

**数据维护策略**：
- **新增错题**：练习提交或考试交卷时，若 `is_correct = 0`，则 upsert 到 `t_wrong_book`（`wrong_count + 1`，更新 `last_wrong_time`）
- **再次做对**：学生在错题重练中做对了，在错题本中该题 `wrong_count` 不变但新增 `correct_count`，`correct_count >= 2`（连续做对 2 次）可标记为 `is_mastered = 1`
- **手动标记**：学生可手动将错题标记为"已掌握"

### 3.4 后端 API 设计

| 方法 | URL | 权限 | 说明 |
|------|-----|------|------|
| GET | `/api/student/wrong-book` | student | 获取错题列表（分页，支持 courseId/questionType/difficulty/isMastered 筛选） |
| GET | `/api/student/wrong-book/stats` | student | 错题统计（按课程分组：各课程错题数） |
| DELETE | `/api/student/wrong-book/{id}` | student | 移除单条错题（标记 is_mastered=1） |
| DELETE | `/api/student/wrong-book/batch` | student | 批量移除错题 |
| POST | `/api/student/wrong-book/practice` | student | 错题重练——生成练习题目（入参同练习生成，但从错题本选题） |

**错题重练流程**：
1. 调用 `POST /api/student/wrong-book/practice` 生成题目（与现有练习生成接口类似，但题目来自错题本）
2. 使用现有 `POST /api/student/practice/submit` 提交答案
3. 后端判定：若做对了，在 `t_wrong_book` 中记录 `correct_count + 1`。若 `correct_count >= 2`，自动标记 `is_mastered = 1`
4. 前端展示结果（复用现有练习反馈逻辑）

### 3.5 前端设计

**错题本页面**（新建 `student_wrong_book.html`）：

```
┌─────────────────────────────────────────────────────────┐
│  📋 我的错题本                                          │
│                                                         │
│  筛选栏：课程[全部 ▼] 题型[全部 ▼] 难度[全部 ▼]         │
│          状态[未掌握 / 已掌握 / 全部]                    │
│                                                         │
│  统计卡片：📊 全部 25题 | ❌ 未掌握 18题 | ✅ 已掌握 7题 │
│                                                         │
│  ┌─────────────────────────────────────────────────────┐│
│  │ #1 [Java程序设计] [单选题] [难度:易]                  ││
│  │ Q: Java中定义一个类使用以下哪个关键字？               ││
│  │ 做错 3 次 | 最近错误: 2026-07-30 | 首次错误: 2026-07-10 ││
│  │ [重新练习] [标记已掌握]                              ││
│  └─────────────────────────────────────────────────────┘│
│  ┌─────────────────────────────────────────────────────┐│
│  │ #2 [数据库原理] [多选题] [难度:中]                    ││
│  │ ...                                                  ││
│  └─────────────────────────────────────────────────────┘│
│                                                         │
│  分页：< 1 2 3 >                                       │
└─────────────────────────────────────────────────────────┘
```

- 点击"重新练习"→ 进入错题练习模式（可批量选择，或逐个练习）
- 练习模式复用现有 `student_practice.html` 的交互流程（确认答案 → 查看解析 → 下一题）
- 错题练习模式顶部显示"错题重练模式"标签，以示区分
- 做对的题自动从当前错题列表移除（`is_mastered`），底部显示本次正确数

**入口**：学生 dashboard 侧边栏增加"错题本"菜单项，dashboard 首页 KPI 卡片增加"错题数"

### 3.6 技术要点

- 错题本题库来自 `t_wrong_book` JOIN `t_question`，查询时需要排除 `is_mastered = 1` 的题目（除非学生主动切换筛选）
- 错题重练提交时，需在 `PracticeServiceImpl.submitAnswer()` 中新增逻辑：查询该题是否在错题本中，若在且做对了则更新 `correct_count`
- 现有 `t_practice_record` 不做任何修改，错题本通过 `t_wrong_book` 独立追踪
- 练习提交和考试交卷时同步更新 `t_wrong_book`，不异步

---

## 四、功能模块三：试卷导出

### 4.1 需求说明

教师需要将试卷导出为可打印的文件格式：
- **导出为 Word（.docx）**：格式清晰，教师可编辑修改
- **导出为 PDF**：格式固定，适合直接打印
- **两种版本**：含答案版（教师用）+ 不含答案版（发给学生做纸质试卷）
- **内容包含**：试卷标题、考试信息（时长/总分）、每题题干 + 选项 + 分值、答题区域（填空/简答留白）

### 4.2 技术选型

| 方案 | 说明 | 优缺点 |
|------|------|--------|
| Apache POI | Java 生成 .docx 文件 | ✅ 纯 Java，无需外部依赖 ✅ 生成的 .docx 可用 Word 打开编辑 ❌ API 较底层，排版复杂 |
| iText / Flying Saucer | Java 生成 PDF（HTML → PDF） | ✅ 排版灵活，可将 HTML 模板转 PDF ❌ iText 商业许可需注意（AGPL） |
| **推荐方案** | Apache POI 生成 Word，浏览器打印生成 PDF | ✅ POI 做 .docx，学生端通过浏览器"打印 → 另存为 PDF"生成 PDF；或后端用 OpenPDF（iText 的 LGPL fork）做 PDF |

> **建议**：先用 Apache POI 实现 Word 导出，PDF 通过浏览器打印实现。如需后端直接生成 PDF，再引入 OpenPDF（LGPL，无许可问题）。

**Maven 依赖**：
```xml
<!-- Word 导出 -->
<dependency>
    <groupId>org.apache.poi</groupId>
    <artifactId>poi-ooxml</artifactId>
    <version>5.4.0</version>
</dependency>
```

### 4.3 后端 API 设计

| 方法 | URL | 权限 | 说明 |
|------|-----|------|------|
| GET | `/api/teacher/papers/{id}/export` | teacher | 导出试卷文件 |
| GET | `/api/student/exams/{examRecordId}/export` | student | 导出自己的考试答题记录（含得分、答案解析） |

**请求参数**（教师导出）：

| 参数 | 类型 | 必填 | 默认值 | 说明 |
|------|------|------|--------|------|
| format | String | 否 | docx | 导出格式：`docx` / `pdf` |
| withAnswer | Boolean | 否 | true | 是否包含答案：`true` 含答案版，`false` 学生版 |

**响应**：`Content-Type: application/octet-stream`，`Content-Disposition: attachment; filename="试卷名.docx"`，直接返回文件二进制流。

### 4.4 导出内容模板

**含答案版（教师用）**：
```
═══════════════════════════════════
        Java程序设计 第一章测验
课程：Java程序设计 | 时长：45分钟 | 总分：100分
═══════════════════════════════════

一、单选题（每题10分，共2题）
─────────────────────────────────
1. Java中定义一个类使用以下哪个关键字？
   A. class        B. struct
   C. define       D. object
   【答案】A
   【解析】class 是Java中定义类的关键字。

2. ...
═══════════════════════════════════
```

**不含答案版（学生版）**：
```
═══════════════════════════════════
        Java程序设计 第一章测验
课程：Java程序设计 | 时长：45分钟 | 总分：100分
班级：__________  姓名：__________  得分：__________
═══════════════════════════════════

一、单选题（每题10分，共2题）
─────────────────────────────────
1. Java中定义一个类使用以下哪个关键字？
   A. class        B. struct
   C. define       D. object

2. ...
═══════════════════════════════════
```

### 4.5 前端设计

- **教师端**：试卷列表或试卷详情页，增加"导出"按钮，点击弹出格式选择下拉菜单
  ```
  [导出 ▼]
    ├── Word（含答案）
    ├── Word（学生版）
    ├── PDF（含答案）
    └── PDF（学生版）
  ```
- **学生端**：考试结果页，增加"导出答题记录"按钮，导出含本人答案、得分、正确答案、解析的 PDF/Word

### 4.6 技术要点

- 试卷导出不依赖前端 HTML 渲染，纯后端生成文件
- 填空/简答题需要留出答题区域（空白行）
- 判断题自动补充"对""错"选项
- 中文内容注意字体设置（宋体/黑体），避免导出乱码
- 带答案版中正确答案需有明显标识（加粗/下划线）

---

## 五、体验优化

### 5.1 修复已知死数据/占位符（🔴 优先）

以下为代码审查中发现的硬编码占位符，v2.0 必须修复：

| 问题 | 文件 | 修复方案 |
|------|------|----------|
| 累计错题显示 `'-- 题'` | `student_results.html:238` | 后端新增统计接口或在错题本模块中实现后接入 |
| 考试排名始终 `'--'` | `student_results.html` + 后端 | 后端 `ExamRecordMapper` 已有 `ORDER BY total_score DESC`，在 `ExamResultVO` 中补 `setRank()` |
| 平均正确率/平均测验分 `'--'` | `student_dashboard.html` | 后端 `DashboardStatsVO` 中添加 `avgAccuracy`/`avgScore` 字段并计算 |
| 教师 dashboard 进度条 0/0 | `teacher_dashboard.html` | 后端 `DashboardStatsVO` 添加 `submittedCount`/`totalStudentCount` |
| 待批阅简答题空占位 | `teacher_dashboard.html` | v2.0 可移除或实现简单的教师批阅流程 |

### 5.2 加载状态与空状态

**现状问题**：所有页面使用 `alert()` 弹窗报错，没有 loading 动画，数据加载中页面空白，用户不知道系统在做什么。

**优化方案**：

| 优化项 | 说明 |
|--------|------|
| **全局 Loading** | 所有 API 调用期间显示顶部进度条（类似 YouTube 红色进度条）或骨架屏 |
| **按钮 Loading** | 提交按钮点击后显示 spinner + "提交中..."，禁止重复点击 |
| **空状态** | 列表无数据时显示友好的空状态插图和提示文字（如 📭 "暂无题目"），而非空白 |
| **Toast 通知** | 操作成功/失败用顶部浮层提示（2秒自动消失），替代 `alert()` |
| **确认弹窗** | 删除等危险操作用自定义确认模态框，替代浏览器 `confirm()` |

**实现方式**：编写通用 JS 工具函数（放在公共的 `common.js` 或内联在各页面中）：
```javascript
// Toast 通知
function showToast(message, type) { /* type: success/error/warning/info */ }

// 确认弹窗
function showConfirm(message, onConfirm) { /* 返回 Promise */ }

// Loading 切换
function showLoading(selector) { /* 在指定区域显示骨架屏 */ }
function hideLoading(selector) { /* 隐藏骨架屏 */ }
```

### 5.3 错误处理统一化

**现状问题**：每个 ajax/fetch 调用需要手动 `.then(res => { if (res.code !== 200) { alert(...); return; } ... })`，容易遗漏。

**优化方案**：封装 `apiGet()` / `apiPost()` 函数，统一处理 HTTP 错误和业务错误：

```javascript
async function apiRequest(url, options = {}) {
    const res = await fetch(url, { credentials: 'include', ...options });
    if (!res.ok) {
        if (res.status === 401) { window.location.href = 'login.html'; return; }
        throw new Error('网络错误: ' + res.status);
    }
    const json = await res.json();
    if (json.code !== 200) {
        showToast(json.message || '操作失败', 'error');
        throw new Error(json.message);
    }
    return json.data;
}
```

> **注意**：此改动涉及所有页面的 API 调用方式，需逐页重构。建议逐个页面迁移，不一次性全改。

### 5.4 教师端优化

| 优化项 | 说明 | 优先级 |
|--------|------|--------|
| **题目批量导入** | 支持 Excel/CSV 批量导入题目（提供模板下载） | 🟡 中 |
| **题目批量删除** | 题目列表支持多选 + 批量删除 | 🟡 中 |
| **题目预览** | 题目列表项点击展开预览完整题目（含选项），不进入编辑页 | 🟢 低 |
| **试卷预览** | 发布前整卷预览（模拟学生视角），确保题目和分值正确 | 🔴 高 |
| **组卷优化** | 从题库选入试卷时支持按题型/难度筛选 + 批量添加 | 🟡 中 |
| **题目搜索增强** | 支持按课程 + 题型 + 难度 + 关键词组合搜索（当前已有，确认是否完整） | 🟢 低 |
| **Dashboard 图表** | 教师 dashboard 增加图表：学生成绩分布饼图、各课程题目数量柱状图 | 🟡 中 |

### 5.5 学生端优化

| 优化项 | 说明 | 优先级 |
|--------|------|--------|
| **考试倒计时优化** | 剩余 5 分钟时倒计时变红闪烁提醒；时间到自动交卷 | 🔴 高 |
| **考试断线保护** | 考试过程中网络断开时，已选答案保留在本地（localStorage），恢复后继续 | 🟡 中 |
| **练习进度显示** | 练习页面顶部显示进度条 "第 3/10 题" | 🟡 中 |
| **练习正确率统计** | 练习完成后显示本次练习正确率和用时 | 🟢 低 |
| **Dashboard 丰富** | 学生 dashboard 增加：最近练习正确率趋势图、即将截止的考试提醒 | 🟡 中 |

### 5.6 管理员端优化

| 优化项 | 说明 | 优先级 |
|--------|------|--------|
| **批量导入学生** | 支持 Excel 批量导入学生账号（班级 + 姓名 + 学号） | 🟡 中 |
| **操作日志** | 记录管理员的关键操作（增删账号、分配课程等），`t_operation_log` 表 | 🟢 低 |
| **数据统计增强** | 管理员 dashboard 增加图表和更多统计维度 | 🟢 低 |

### 5.7 通用体验优化

| 优化项 | 说明 |
|--------|------|
| **响应式适配** | 当前为桌面端设计，需适配平板（iPad）横屏，暂不做手机端（屏幕太小不适合做题） |
| **键盘快捷键** | 练习/考试页面支持键盘操作：A/B/C/D 选择答案，Enter 确认，→ 下一题 |
| **页面过渡** | 页面间导航增加平滑过渡（可选，CSS transition） |
| **字体大小可调** | 题目内容区提供字体放大/缩小按钮，方便阅读 |
| **暗色模式** | 暂不做，Tailwind CSS 3.x via CDN 支持 dark mode 但工程量大 |

### 5.8 代码质量优化

| 优化项 | 说明 |
|--------|------|
| **前端代码去重** | 多个页面存在重复的 `getCurrentUser()`、`apiGet()`、`showToast()` 等函数，抽取到 `common.js` 公共文件 |
| **后端代码整理** | `PracticeServiceImpl` 和 `ExamServiceImpl` 中判断题选项自动生成逻辑重复，抽取为公共方法 |
| **前端 JS 模块化** | 当前全为内联 `<script>` 标签，考虑拆分独立的 `.js` 文件（或保持内联以减少请求数） |

---

## 六、数据库变更汇总

v2.0 需要新增 **3 张表**：

| 表名 | 说明 | 所属模块 |
|------|------|----------|
| `t_notification` | 通知消息表 | 通知系统 |
| `t_notification_read` | 通知已读状态表（全局通知用） | 通知系统 |
| `t_wrong_book` | 错题本表 | 错题本 |

**现有表不需要修改**。所有新增表使用独立的主键和 FK，不影响现有数据。

DDL 脚本需要更新到 `resources/db/schema.sql`。

---

## 七、新增 API 接口汇总

v2.0 新增 **约 15 个接口**：

### 7.1 通知模块（6 个）

| 方法 | URL | 权限 | 说明 |
|------|-----|------|------|
| POST | `/api/admin/notifications` | admin | 发布系统公告 |
| GET | `/api/notifications` | 登录 | 获取通知列表（分页） |
| GET | `/api/notifications/unread-count` | 登录 | 获取未读数量 |
| PUT | `/api/notifications/{id}/read` | 登录 | 标记已读 |
| PUT | `/api/notifications/read-all` | 登录 | 全部已读 |
| DELETE | `/api/notifications/{id}` | 登录 | 删除通知 |

### 7.2 错题本模块（5 个）

| 方法 | URL | 权限 | 说明 |
|------|-----|------|------|
| GET | `/api/student/wrong-book` | student | 错题列表（分页+筛选） |
| GET | `/api/student/wrong-book/stats` | student | 错题统计 |
| DELETE | `/api/student/wrong-book/{id}` | student | 移除单条错题 |
| DELETE | `/api/student/wrong-book/batch` | student | 批量移除 |
| POST | `/api/student/wrong-book/practice` | student | 错题重练生成 |

### 7.3 试卷导出模块（2 个）

| 方法 | URL | 权限 | 说明 |
|------|-----|------|------|
| GET | `/api/teacher/papers/{id}/export` | teacher | 导出试卷（含参：format/withAnswer） |
| GET | `/api/student/exams/{examRecordId}/export` | student | 导出答题记录 |

### 7.4 后端改动点

| 文件 | 改动 |
|------|------|
| `PracticeServiceImpl.submitAnswer()` | 新增逻辑：提交答案后若做错 → 写入/更新 `t_wrong_book`；若做对且在错题本中 → 更新 `correct_count` |
| `ExamServiceImpl.submitExam()` | 新增逻辑：交卷后遍历所有答错的题 → 写入/更新 `t_wrong_book`；发布/回收试卷 → 创建通知 |
| `PaperServiceImpl.publishPaper()` | 新增逻辑：发布后为选修该课程的学生创建考试通知 |
| `PaperServiceImpl.recallPaper()` | 新增逻辑：回收后为相关学生创建回收通知 |
| 新增 `NotificationController` | 通知 CRUD 接口 |
| 新增 `NotificationService` / `NotificationServiceImpl` | 通知业务逻辑 |
| 新增 `WrongBookController` | 错题本接口（亦可扩展 `StudentPracticeController`） |
| 新增 `WrongBookService` / `WrongBookServiceImpl` | 错题本逻辑 |
| 新增 `PaperExportService` / `PaperExportServiceImpl` | 试卷导出（Word/PDF 生成） |
| 新增 Mapper + XML | `NotificationMapper`、`NotificationReadMapper`、`WrongBookMapper` |

---

## 八、实施路线图

建议按以下阶段推进，每阶段可独立测试验证：

### 阶段 1：通知系统（预计 3-4 天）

- [ ] 1.1 数据库建表 `t_notification` + `t_notification_read`
- [ ] 1.2 后端：`NotificationController` + `NotificationService` + Mapper
- [ ] 1.3 后端：在 `PaperServiceImpl` 和 `ExamServiceImpl` 中埋入通知创建逻辑
- [ ] 1.4 前端：各页面顶部栏增加通知铃铛 + 未读徽标
- [ ] 1.5 前端：通知中心页面 `notifications.html`
- [ ] 1.6 测试：发布试卷 → 学生收到通知；交卷 → 成绩通知

### 阶段 2：错题本（预计 3-4 天）

- [ ] 2.1 数据库建表 `t_wrong_book`
- [ ] 2.2 后端：`WrongBookController` + `WrongBookService` + Mapper（列表/统计/移除/重练）
- [ ] 2.3 后端：在 `PracticeServiceImpl.submitAnswer()` 和 `ExamServiceImpl.submitExam()` 中埋入错题收录 + 掌握判定逻辑
- [ ] 2.4 前端：错题本页面 `student_wrong_book.html`（列表 + 筛选 + 统计卡片）
- [ ] 2.5 前端：学生 dashboard 侧边栏增加入口 + KPI 卡片
- [ ] 2.6 测试：练习做错 → 错题本收录；重练做对 ≥2 次 → 标记掌握

### 阶段 3：试卷导出（预计 2-3 天）

- [ ] 3.1 添加 Apache POI 依赖
- [ ] 3.2 后端：`PaperExportService` 实现 Word 文档生成（含答案/不含答案）
- [ ] 3.3 后端：`PaperExportController` 导出接口
- [ ] 3.4 前端：教师试卷列表增加"导出"按钮
- [ ] 3.5 前端：学生考试结果页增加"导出答题记录"按钮
- [ ] 3.6 测试：导出文件内容正确，中文无乱码

### 阶段 4：体验优化（预计 3-5 天）

- [ ] 4.1 封装 `common.js`：抽取 `showToast()`、`showConfirm()`、`apiRequest()` 等公共函数
- [ ] 4.2 各页面逐步迁移到统一 API 请求封装（非一次性，分页面推进）
- [ ] 4.3 全局 Loading 动画 + 空状态展示
- [ ] 4.4 考试倒计时优化（5分钟提醒 + 自动交卷）
- [ ] 4.5 教师试卷预览功能
- [ ] 4.6 教师 dashboard 图表（可选，若时间允许）
- [ ] 4.7 正则表达式扫一遍所有 HTML 页面 JS 语法

### 阶段 5：集成测试与文档（预计 1-2 天）

- [ ] 5.1 全流程回归测试（三端所有功能）
- [ ] 5.2 更新 `API接口文档.md`
- [ ] 5.3 更新 `02_数据库设计.md` 或标记为废弃
- [ ] 5.4 更新 `schema.sql`
- [ ] 5.5 更新 `CLAUDE.md`

> **总预计工作量**：12-18 天（1-2 人全职）

---

## 九、风险与注意事项

### 9.1 技术风险

| 风险 | 影响 | 缓解措施 |
|------|------|----------|
| Apache POI 排版复杂 | Word 导出格式不理想 | 先用简单排版实现基础版，后续迭代美化 |
| 错题本数据一致性 | 删除题目后错题本出现悬挂引用 | `t_question` 删除时同步删除 `t_wrong_book` 中对应记录（FK CASCADE 或手动删除） |
| 通知数据膨胀 | 长期运行后通知表过大 | v2.0 暂不处理；后续可加定时清理 90 天前的已读通知 |
| 前端代码重构风险 | 统一 API 封装可能引入新 bug | 逐页迁移，每改一页验证一页，不做大面积替换 |

### 9.2 兼容性

- **后端**：所有新增接口不修改现有接口的请求/响应格式
- **数据库**：新增表不修改现有表结构，现有 SQL 不受影响
- **前端**：新增通知铃铛组件，不破坏现有导航栏布局
- **已有的 6 个默认账号不需变动**

### 9.3 后续版本展望（v3.0+）

以下功能留待后续版本实现：

- **即时通讯（聊天）**：WebSocket 实时通信，师生一对一答疑
- **邮件/短信通知**：对接外部通知渠道
- **AI 辅助出题**：根据知识点自动生成题目
- **知识点体系**：`t_knowledge_point` 表，题目关联知识点，按知识点统计薄弱项
- **多语言支持**：i18n 国际化
- **移动端 App / 小程序**：原生移动端体验

---

> 📅 **文档版本**：v1.0 | **编写日期**：2026-07-31 | **编写人**：开发团队
